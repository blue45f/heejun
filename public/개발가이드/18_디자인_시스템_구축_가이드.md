# 디자인 시스템 구축 가이드 2026 -- AI 활용 극대화 + 멀티 베타 환경

## 목차

1. [AI 프롬프트 7선 -- 디자인 시스템 자동화](#1-ai-프롬프트-7선----디자인-시스템-자동화)
2. [멀티 베타 환경 -- Storybook 자동 배포와 테마 전환](#2-멀티-베타-환경----storybook-자동-배포와-테마-전환)
3. [Radix UI / shadcn/ui / Ark UI 비교](#3-radix-ui--shadcnui--ark-ui-비교)
4. [디자인 토큰 3계층 시스템](#4-디자인-토큰-3계층-시스템)
5. [컴포넌트 설계 패턴 -- Compound, Polymorphic, Slot](#5-컴포넌트-설계-패턴----compound-polymorphic-slot)
6. [Storybook 8 + Chromatic Visual Regression](#6-storybook-8--chromatic-visual-regression)
7. [Changesets 버전 관리 + 배포 전략](#7-changesets-버전-관리--배포-전략)
8. [Tailwind CSS v4 + CSS 변수 통합](#8-tailwind-css-v4--css-변수-통합)
9. [참고 자료](#9-참고-자료)

---

## 1. AI 프롬프트 7선 -- 디자인 시스템 자동화

### 프롬프트 1: Compound Component 패턴 생성

> ```
> 다음 요구사항에 맞는 React Compound Component를 생성해줘.
> - 컴포넌트: {컴포넌트명} (예: Accordion, Tabs, Select)
> - Radix UI 프리미티브 기반
> - Tailwind CSS v4 스타일링 (CSS 변수 활용)
> - TypeScript 제네릭 지원
> - 포함: Root, Trigger, Content, Item 서브 컴포넌트
> - Context Provider로 상태 공유
> - ARIA 접근성 완전 준수
> - forwardRef + displayName 설정
> - Storybook 스토리 포함 (Default, AllVariants, Interactive)
> - Vitest + Testing Library 테스트 포함
>
> --- 디자인 토큰 ---
> {프로젝트의 디자인 토큰 붙여넣기}
> ```

### 프롬프트 2: Polymorphic 컴포넌트 변환

> ```
> 다음 기본 컴포넌트를 Polymorphic 패턴으로 변환해줘.
> - as prop으로 렌더링 요소를 변경 가능하게
> - 각 요소 타입에 맞는 HTML 속성이 TypeScript로 자동 완성
> - ComponentPropsWithoutRef + 제네릭 활용
> - asChild prop도 지원 (Radix Slot 패턴)
> - 기본 요소 타입 설정 가능
>
> --- 기본 컴포넌트 코드 ---
> {코드 붙여넣기}
> ```

### 프롬프트 3: CVA Variant 시스템 생성

> ```
> 다음 디자인 토큰과 컴포넌트 스펙을 기반으로
> class-variance-authority(CVA)를 사용한 variant 시스템을 구현해줘.
> variant 구성:
> - size: sm / md / lg / xl
> - variant: primary / secondary / ghost / destructive / outline
> - compound variant: disabled + 각 variant 조합
> - defaultVariants 설정
> Tailwind CSS v4 클래스와 CSS 변수를 사용해줘.
>
> --- 디자인 스펙 ---
> {Figma 스펙 또는 디자인 토큰 붙여넣기}
> ```

### 프롬프트 4: 기존 컴포넌트 리팩토링

> ```
> 다음 컴포넌트를 디자인 시스템 표준에 맞게 리팩토링해줘.
> 적용할 패턴:
> - forwardRef + displayName
> - Slot 패턴 (asChild prop)
> - data-* 속성 기반 상태 스타일링 (data-state, data-disabled)
> - CSS 변수 기반 커스터마이징 (--component-*)
> - 적절한 기본값과 타입 안전성
> - cn() 유틸리티로 클래스 병합
>
> --- 현재 컴포넌트 ---
> {코드 붙여넣기}
> ```

### 프롬프트 5: 테스트 + 스토리 일괄 생성

> ```
> 다음 컴포넌트에 대해 아래 항목을 모두 생성해줘.
> 1. Vitest + Testing Library 단위 테스트
>    - 렌더링, 인터랙션, 접근성, 스냅샷
> 2. Storybook 스토리 4종
>    - Default: 기본 상태
>    - AllVariants: 모든 variant/size 조합 그리드
>    - Interactive: play 함수로 인터랙션 시나리오
>    - Playground: controls로 모든 props 조작
> 3. JSDoc 주석 (Storybook docs 자동 생성용)
>
> --- 컴포넌트 코드 ---
> {코드 붙여넣기}
> ```

### 프롬프트 6: Figma 디자인 토큰 추출

> ```
> 다음 Figma REST API 응답(스타일 데이터)을 분석해서
> 3계층 디자인 토큰 코드를 생성해줘.
> - Primitive: 원시 색상/크기 값
> - Semantic: 의미 기반 토큰 (--color-primary, --spacing-md)
> - Component: 컴포넌트별 토큰 (--button-bg, --input-border)
> 출력 형식: CSS 변수 + TypeScript 타입 정의
>
> --- Figma API 응답 ---
> {API 응답 JSON 붙여넣기}
> ```

### 프롬프트 7: 컴포넌트 API 설계 리뷰

> ```
> 다음 컴포넌트의 API(props) 설계를 리뷰해줘.
> 점검 항목:
> - Props 네이밍 일관성 (on* 이벤트, is*/has* 불리언)
> - 필수/선택 props 적절성
> - 기본값 합리성
> - 타입 안전성 (유니온 vs enum, 제네릭)
> - 확장성 (향후 기능 추가 시 breaking change 여부)
> - 다른 라이브러리(Radix, MUI, Ant) 대비 관례 준수
> - children vs render prop vs slot 패턴 적합성
>
> 개선 제안을 before/after 코드로 보여줘.
>
> --- 컴포넌트 API ---
> {타입 정의 붙여넣기}
> ```

---

## 2. 멀티 베타 환경 -- Storybook 자동 배포와 테마 전환

### 2.1 Preview 환경별 Storybook 자동 배포

```yaml
# .github/workflows/storybook-preview.yml
name: Storybook Multi-Beta Deploy
on:
  pull_request:
    branches: [main, "beta-*", "release-*"]

jobs:
  storybook-deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        theme: [light, dark]
        env: [alpha, beta, rc]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - name: Build Storybook (${{ matrix.env }} / ${{ matrix.theme }})
        run: npx storybook build -o storybook-static-${{ matrix.env }}-${{ matrix.theme }}
        env:
          STORYBOOK_ENV: ${{ matrix.env }}
          STORYBOOK_THEME: ${{ matrix.theme }}
          STORYBOOK_TOKENS: ${{ matrix.env }}

      - name: Deploy to preview
        uses: actions/upload-artifact@v4
        with:
          name: storybook-${{ matrix.env }}-${{ matrix.theme }}
          path: storybook-static-${{ matrix.env }}-${{ matrix.theme }}/

      - name: Comment PR with preview URLs
        if: matrix.theme == 'light' && matrix.env == 'beta'
        uses: actions/github-script@v7
        with:
          script: |
            const envs = ['alpha', 'beta', 'rc'];
            const themes = ['light', 'dark'];
            let body = '## Storybook Preview URLs\n\n';
            body += '| 환경 | Light | Dark |\n';
            body += '|------|-------|------|\n';
            for (const env of envs) {
              body += `| ${env} | [Light](${process.env.PREVIEW_BASE_URL}/${env}/light) | [Dark](${process.env.PREVIEW_BASE_URL}/${env}/dark) |\n`;
            }
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body
            });
```

### 2.2 환경별 테마/토큰 전환

```typescript
// .storybook/theme-decorator.tsx
import { useEffect, type ReactNode } from "react";

type ThemeMode = "light" | "dark";
type EnvName = "alpha" | "beta" | "rc" | "production";

interface TokenSet {
  colors: Record<string, string>;
  spacing: Record<string, string>;
  radii: Record<string, string>;
}

const ENV_TOKEN_OVERRIDES: Record<EnvName, Partial<TokenSet>> = {
  alpha: {
    colors: { "--color-primary": "#6366f1", "--color-accent": "#f59e0b" },
  },
  beta: {
    colors: { "--color-primary": "#3b82f6", "--color-accent": "#10b981" },
  },
  rc: {
    colors: { "--color-primary": "#2563eb", "--color-accent": "#059669" },
  },
  production: {
    colors: { "--color-primary": "#1d4ed8", "--color-accent": "#047857" },
  },
};

export function ThemeProvider({
  children,
  theme,
  env,
}: {
  children: ReactNode;
  theme: ThemeMode;
  env: EnvName;
}) {
  useEffect(() => {
    const root = document.documentElement;
    root.setAttribute("data-theme", theme);
    root.setAttribute("data-env", env);

    const overrides = ENV_TOKEN_OVERRIDES[env];
    if (overrides?.colors) {
      for (const [key, value] of Object.entries(overrides.colors)) {
        root.style.setProperty(key, value);
      }
    }

    return () => {
      root.removeAttribute("data-theme");
      root.removeAttribute("data-env");
    };
  }, [theme, env]);

  return <>{children}</>;
}
```

```typescript
// .storybook/preview.tsx
import type { Preview } from "@storybook/react";
import { ThemeProvider } from "./theme-decorator";

const preview: Preview = {
  globalTypes: {
    theme: {
      description: "테마 모드",
      toolbar: {
        title: "Theme",
        items: [
          { value: "light", title: "Light", icon: "sun" },
          { value: "dark", title: "Dark", icon: "moon" },
        ],
        dynamicTitle: true,
      },
    },
    env: {
      description: "배포 환경",
      toolbar: {
        title: "Environment",
        items: [
          { value: "alpha", title: "Alpha" },
          { value: "beta", title: "Beta" },
          { value: "rc", title: "RC" },
          { value: "production", title: "Production" },
        ],
        dynamicTitle: true,
      },
    },
  },
  initialGlobals: {
    theme: process.env.STORYBOOK_THEME || "light",
    env: process.env.STORYBOOK_ENV || "production",
  },
  decorators: [
    (Story, context) => (
      <ThemeProvider theme={context.globals.theme} env={context.globals.env}>
        <Story />
      </ThemeProvider>
    ),
  ],
};

export default preview;
```

### 2.3 Chromatic Visual Regression per PR

```yaml
# .github/workflows/chromatic.yml
name: Chromatic Visual Regression
on:
  pull_request:
    branches: [main, "beta-*"]

jobs:
  chromatic:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: [beta, rc, production]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - name: Chromatic (${{ matrix.env }})
        uses: chromaui/action@latest
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          buildScriptName: build-storybook
          exitOnceUploaded: true
        env:
          STORYBOOK_ENV: ${{ matrix.env }}
          STORYBOOK_THEME: light
```

---

## 3. Radix UI / shadcn/ui / Ark UI 비교

| 기준 | Radix UI | shadcn/ui | Ark UI |
|------|----------|-----------|--------|
| 타입 | Headless 프리미티브 | 복사-붙여넣기 컴포넌트 | Headless (State Machine) |
| 스타일링 | 스타일 없음 | Tailwind CSS 기본 | 스타일 없음 |
| 설치 | npm 패키지 | CLI로 소스 복사 | npm 패키지 |
| 접근성 | WAI-ARIA 완전 준수 | Radix 기반 | WAI-ARIA 완전 준수 |
| 프레임워크 | React 전용 | React / Next.js | React, Vue, Solid |
| 커스터마이징 | 자유도 높음 | 소스 수정 자유 | 자유도 높음 |
| 번들 크기 | 작음 (트리셰이킹) | 사용한 것만 포함 | 중간 (state machine) |
| SSR | 지원 | 지원 | 지원 |
| 애니메이션 | data-state 기반 | Tailwind 애니메이션 | data-state 기반 |
| 추천 상황 | 커스텀 디자인 시스템 | 빠른 프로토타이핑 | 멀티 프레임워크 |

### 3.1 shadcn/ui 기반 확장 패턴

```typescript
// components/ui/button.tsx
import { forwardRef, type ComponentPropsWithoutRef } from "react";
import { Slot } from "@radix-ui/react-slot";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "@/lib/utils";

const buttonVariants = cva(
  [
    "inline-flex items-center justify-center gap-2",
    "rounded-md text-sm font-medium",
    "ring-offset-background transition-colors",
    "focus-visible:outline-none focus-visible:ring-2",
    "focus-visible:ring-ring focus-visible:ring-offset-2",
    "disabled:pointer-events-none disabled:opacity-50",
    "[&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0",
  ],
  {
    variants: {
      variant: {
        primary: "bg-primary text-primary-foreground hover:bg-primary/90",
        secondary: "bg-secondary text-secondary-foreground hover:bg-secondary/80",
        destructive: "bg-destructive text-destructive-foreground hover:bg-destructive/90",
        ghost: "hover:bg-accent hover:text-accent-foreground",
        outline: "border border-input bg-background hover:bg-accent hover:text-accent-foreground",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        sm: "h-8 px-3 text-xs",
        md: "h-10 px-4 py-2",
        lg: "h-12 px-6 text-base",
        xl: "h-14 px-8 text-lg",
        icon: "size-10",
      },
    },
    compoundVariants: [
      {
        variant: "primary",
        size: "sm",
        className: "shadow-sm",
      },
    ],
    defaultVariants: {
      variant: "primary",
      size: "md",
    },
  }
);

interface ButtonProps
  extends ComponentPropsWithoutRef<"button">,
    VariantProps<typeof buttonVariants> {
  /** Slot 패턴: 자식 요소를 렌더링 요소로 사용 */
  asChild?: boolean;
  /** 로딩 상태 */
  loading?: boolean;
}

const Button = forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, asChild = false, loading, disabled, children, ...props }, ref) => {
    const Comp = asChild ? Slot : "button";
    return (
      <Comp
        ref={ref}
        className={cn(buttonVariants({ variant, size, className }))}
        disabled={disabled || loading}
        aria-busy={loading || undefined}
        data-loading={loading || undefined}
        {...props}
      >
        {loading ? (
          <>
            <span className="animate-spin" aria-hidden="true">
              &#9696;
            </span>
            <span className="sr-only">로딩 중</span>
            {children}
          </>
        ) : (
          children
        )}
      </Comp>
    );
  }
);
Button.displayName = "Button";

export { Button, buttonVariants, type ButtonProps };
```

---

## 4. 디자인 토큰 3계층 시스템

### 4.1 계층 구조

```
Primitive (원시값)
  --> Semantic (의미 기반)
    --> Component (컴포넌트 전용)
```

### 4.2 Primitive 토큰 -- 원시값

```css
/* tokens/primitive.css */
:root {
  /* Colors -- Raw palette */
  --gray-50: oklch(0.985 0 0);
  --gray-100: oklch(0.967 0.001 286);
  --gray-200: oklch(0.928 0.006 286);
  --gray-300: oklch(0.872 0.01 286);
  --gray-400: oklch(0.707 0.015 286);
  --gray-500: oklch(0.551 0.018 286);
  --gray-600: oklch(0.446 0.018 286);
  --gray-700: oklch(0.372 0.016 286);
  --gray-800: oklch(0.279 0.012 286);
  --gray-900: oklch(0.21 0.006 286);
  --gray-950: oklch(0.145 0.004 286);

  --blue-50: oklch(0.97 0.014 254);
  --blue-500: oklch(0.623 0.214 259);
  --blue-600: oklch(0.546 0.245 262);
  --blue-700: oklch(0.488 0.243 264);

  --red-500: oklch(0.577 0.245 27);
  --green-500: oklch(0.723 0.219 149);
  --amber-500: oklch(0.769 0.188 70);

  /* Spacing */
  --space-0: 0px;
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;

  /* Border Radius */
  --radius-none: 0px;
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;
  --radius-xl: 12px;
  --radius-full: 9999px;

  /* Font Size */
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.25rem;
  --text-2xl: 1.5rem;
  --text-3xl: 1.875rem;

  /* Shadow */
  --shadow-sm: 0 1px 2px oklch(0 0 0 / 0.05);
  --shadow-md: 0 4px 6px -1px oklch(0 0 0 / 0.1);
  --shadow-lg: 0 10px 15px -3px oklch(0 0 0 / 0.1);
}
```

### 4.3 Semantic 토큰 -- 의미 기반

```css
/* tokens/semantic.css */
:root,
[data-theme="light"] {
  /* Background */
  --color-bg: var(--gray-50);
  --color-bg-subtle: var(--gray-100);
  --color-bg-muted: var(--gray-200);
  --color-bg-inverse: var(--gray-900);

  /* Foreground */
  --color-fg: var(--gray-900);
  --color-fg-muted: var(--gray-600);
  --color-fg-subtle: var(--gray-400);
  --color-fg-inverse: var(--gray-50);

  /* Brand */
  --color-primary: var(--blue-600);
  --color-primary-hover: var(--blue-700);
  --color-primary-fg: white;

  /* Semantic */
  --color-success: var(--green-500);
  --color-warning: var(--amber-500);
  --color-error: var(--red-500);

  /* Border */
  --color-border: var(--gray-200);
  --color-border-hover: var(--gray-300);
  --color-border-focus: var(--blue-500);

  /* Focus Ring */
  --color-ring: var(--blue-500);
  --ring-width: 2px;
  --ring-offset: 2px;
}

[data-theme="dark"] {
  --color-bg: var(--gray-950);
  --color-bg-subtle: var(--gray-900);
  --color-bg-muted: var(--gray-800);
  --color-bg-inverse: var(--gray-50);

  --color-fg: var(--gray-50);
  --color-fg-muted: var(--gray-400);
  --color-fg-subtle: var(--gray-500);
  --color-fg-inverse: var(--gray-900);

  --color-primary: var(--blue-500);
  --color-primary-hover: var(--blue-600);
  --color-primary-fg: white;

  --color-border: var(--gray-800);
  --color-border-hover: var(--gray-700);
  --color-border-focus: var(--blue-500);
}
```

### 4.4 Component 토큰 -- 컴포넌트 전용

```css
/* tokens/component.css */
:root {
  /* Button */
  --button-height-sm: var(--space-8);
  --button-height-md: var(--space-10);
  --button-height-lg: var(--space-12);
  --button-px-sm: var(--space-3);
  --button-px-md: var(--space-4);
  --button-px-lg: var(--space-6);
  --button-radius: var(--radius-md);
  --button-font-size: var(--text-sm);
  --button-font-weight: 500;

  /* Input */
  --input-height: var(--space-10);
  --input-px: var(--space-3);
  --input-radius: var(--radius-md);
  --input-border: var(--color-border);
  --input-border-focus: var(--color-border-focus);
  --input-bg: var(--color-bg);
  --input-font-size: var(--text-sm);

  /* Card */
  --card-padding: var(--space-6);
  --card-radius: var(--radius-lg);
  --card-border: var(--color-border);
  --card-bg: var(--color-bg);
  --card-shadow: var(--shadow-sm);

  /* Dialog */
  --dialog-padding: var(--space-6);
  --dialog-radius: var(--radius-xl);
  --dialog-max-width: 32rem;
  --dialog-overlay-bg: oklch(0 0 0 / 0.5);
}
```

### 4.5 TypeScript 토큰 타입 정의

```typescript
// tokens/types.ts
export interface DesignTokens {
  colors: {
    primitive: Record<string, string>;
    semantic: {
      bg: Record<string, string>;
      fg: Record<string, string>;
      primary: Record<string, string>;
      border: Record<string, string>;
      status: Record<string, string>;
    };
  };
  spacing: Record<string, string>;
  radii: Record<string, string>;
  typography: {
    fontSize: Record<string, string>;
    fontWeight: Record<string, number>;
    lineHeight: Record<string, string>;
  };
  shadows: Record<string, string>;
}

export type ColorToken = `--color-${string}`;
export type SpacingToken = `--space-${string}`;
export type RadiusToken = `--radius-${string}`;

/** CSS 변수를 타입 안전하게 참조 */
export function token(name: ColorToken | SpacingToken | RadiusToken): string {
  return `var(${name})`;
}
```

### 4.6 AI 토큰 추출 스크립트

```typescript
// scripts/figma-to-tokens.ts
import { writeFileSync } from "node:fs";

interface FigmaColor {
  r: number;
  g: number;
  b: number;
  a: number;
}

interface FigmaStyle {
  name: string;
  styleType: "FILL" | "TEXT" | "EFFECT" | "GRID";
  description: string;
}

interface FigmaStylesResponse {
  meta: { styles: FigmaStyle[] };
}

async function extractTokensFromFigma(fileKey: string): Promise<void> {
  const apiToken = process.env.FIGMA_API_TOKEN;
  if (!apiToken) throw new Error("FIGMA_API_TOKEN 환경 변수 필요");

  const response = await fetch(`https://api.figma.com/v1/files/${fileKey}/styles`, {
    headers: { "X-Figma-Token": apiToken },
  });

  const data: FigmaStylesResponse = await response.json();

  // AI에게 토큰 변환 요청
  const prompt = `
다음 Figma 스타일 데이터를 3계층 디자인 토큰으로 변환해줘.
출력 형식: CSS 변수 파일 3개 (primitive.css, semantic.css, component.css)

스타일 데이터:
${JSON.stringify(data.meta.styles, null, 2)}
  `.trim();

  const aiResponse = await fetch("https://api.anthropic.com/v1/messages", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "x-api-key": process.env.ANTHROPIC_API_KEY!,
      "anthropic-version": "2024-06-01",
    },
    body: JSON.stringify({
      model: "claude-sonnet-4-20250514",
      max_tokens: 4096,
      messages: [{ role: "user", content: prompt }],
    }),
  });

  const result = await aiResponse.json();
  const content = result.content[0].text;

  // CSS 블록 추출 및 파일 저장
  const cssBlocks = content.matchAll(/```css\n([\s\S]*?)```/g);
  const fileNames = ["primitive.css", "semantic.css", "component.css"];
  let i = 0;

  for (const match of cssBlocks) {
    if (i < fileNames.length) {
      writeFileSync(`tokens/${fileNames[i]}`, match[1].trim());
      i++;
    }
  }

  console.log(`${i}개 토큰 파일 생성 완료`);
}

const fileKey = process.argv[2];
if (!fileKey) {
  console.error("Usage: npx tsx scripts/figma-to-tokens.ts <figma-file-key>");
  process.exit(1);
}
extractTokensFromFigma(fileKey);
```

---

## 5. 컴포넌트 설계 패턴 -- Compound, Polymorphic, Slot

### 5.1 Compound Component 패턴

```typescript
// components/ui/accordion.tsx
import {
  createContext,
  forwardRef,
  useCallback,
  useContext,
  useState,
  type ComponentPropsWithoutRef,
  type ReactNode,
} from "react";
import { cn } from "@/lib/utils";

// --- Context ---
interface AccordionContextValue {
  openItems: Set<string>;
  toggle: (id: string) => void;
  type: "single" | "multiple";
}

const AccordionContext = createContext<AccordionContextValue | null>(null);

function useAccordion() {
  const ctx = useContext(AccordionContext);
  if (!ctx) throw new Error("Accordion 컴포넌트 내에서 사용해야 합니다.");
  return ctx;
}

// --- Root ---
interface AccordionRootProps extends ComponentPropsWithoutRef<"div"> {
  type?: "single" | "multiple";
  defaultOpen?: string[];
}

const AccordionRoot = forwardRef<HTMLDivElement, AccordionRootProps>(
  ({ type = "single", defaultOpen = [], className, children, ...props }, ref) => {
    const [openItems, setOpenItems] = useState<Set<string>>(new Set(defaultOpen));

    const toggle = useCallback(
      (id: string) => {
        setOpenItems((prev) => {
          const next = new Set(prev);
          if (next.has(id)) {
            next.delete(id);
          } else {
            if (type === "single") next.clear();
            next.add(id);
          }
          return next;
        });
      },
      [type]
    );

    return (
      <AccordionContext.Provider value={{ openItems, toggle, type }}>
        <div ref={ref} className={cn("divide-y divide-border", className)} {...props}>
          {children}
        </div>
      </AccordionContext.Provider>
    );
  }
);
AccordionRoot.displayName = "Accordion.Root";

// --- Item ---
interface AccordionItemProps extends ComponentPropsWithoutRef<"div"> {
  value: string;
}

const AccordionItem = forwardRef<HTMLDivElement, AccordionItemProps>(
  ({ value, className, children, ...props }, ref) => {
    const { openItems } = useAccordion();
    const isOpen = openItems.has(value);

    return (
      <div
        ref={ref}
        data-state={isOpen ? "open" : "closed"}
        className={cn("py-2", className)}
        {...props}
      >
        {children}
      </div>
    );
  }
);
AccordionItem.displayName = "Accordion.Item";

// --- Trigger ---
interface AccordionTriggerProps extends ComponentPropsWithoutRef<"button"> {
  value: string;
}

const AccordionTrigger = forwardRef<HTMLButtonElement, AccordionTriggerProps>(
  ({ value, className, children, ...props }, ref) => {
    const { openItems, toggle } = useAccordion();
    const isOpen = openItems.has(value);

    return (
      <button
        ref={ref}
        type="button"
        role="button"
        aria-expanded={isOpen}
        aria-controls={`accordion-content-${value}`}
        onClick={() => toggle(value)}
        className={cn(
          "flex w-full items-center justify-between py-2 text-left font-medium",
          "hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring",
          className
        )}
        {...props}
      >
        {children}
        <span
          className={cn("transition-transform", isOpen && "rotate-180")}
          aria-hidden="true"
        >
          &#9660;
        </span>
      </button>
    );
  }
);
AccordionTrigger.displayName = "Accordion.Trigger";

// --- Content ---
interface AccordionContentProps extends ComponentPropsWithoutRef<"div"> {
  value: string;
}

const AccordionContent = forwardRef<HTMLDivElement, AccordionContentProps>(
  ({ value, className, children, ...props }, ref) => {
    const { openItems } = useAccordion();
    const isOpen = openItems.has(value);

    return (
      <div
        ref={ref}
        id={`accordion-content-${value}`}
        role="region"
        aria-labelledby={`accordion-trigger-${value}`}
        hidden={!isOpen}
        className={cn("overflow-hidden text-sm", isOpen && "pb-4 pt-2", className)}
        {...props}
      >
        {children}
      </div>
    );
  }
);
AccordionContent.displayName = "Accordion.Content";

// --- Export ---
export const Accordion = {
  Root: AccordionRoot,
  Item: AccordionItem,
  Trigger: AccordionTrigger,
  Content: AccordionContent,
};
```

### 5.2 Polymorphic Component 패턴

```typescript
// components/ui/polymorphic.tsx
import {
  forwardRef,
  type ComponentPropsWithoutRef,
  type ElementType,
  type ForwardRefRenderFunction,
  type ReactElement,
} from "react";

/** Polymorphic 컴포넌트의 as prop 타입 */
type AsProp<C extends ElementType> = {
  as?: C;
};

/** as에 따라 유효한 HTML 속성 자동 추론 */
type PolymorphicProps<C extends ElementType, Props = object> = Props &
  AsProp<C> &
  Omit<ComponentPropsWithoutRef<C>, keyof (Props & AsProp<C>)>;

/** Polymorphic forwardRef 헬퍼 */
type PolymorphicRef<C extends ElementType> = ComponentPropsWithoutRef<C>["ref"];

// --- 사용 예시: Text 컴포넌트 ---
type TextVariant = "h1" | "h2" | "h3" | "body" | "caption" | "label";

interface TextOwnProps {
  variant?: TextVariant;
  truncate?: boolean;
}

const VARIANT_STYLES: Record<TextVariant, string> = {
  h1: "text-3xl font-bold tracking-tight",
  h2: "text-2xl font-semibold tracking-tight",
  h3: "text-xl font-semibold",
  body: "text-base",
  caption: "text-sm text-fg-muted",
  label: "text-sm font-medium",
};

const DEFAULT_ELEMENTS: Record<TextVariant, ElementType> = {
  h1: "h1",
  h2: "h2",
  h3: "h3",
  body: "p",
  caption: "span",
  label: "label",
};

type TextProps<C extends ElementType = "p"> = PolymorphicProps<C, TextOwnProps>;

function TextComponent<C extends ElementType = "p">(
  { as, variant = "body", truncate, className, ...props }: TextProps<C>,
  ref: PolymorphicRef<C>
) {
  const Component = as || DEFAULT_ELEMENTS[variant];
  const classes = [
    VARIANT_STYLES[variant],
    truncate && "truncate",
    className,
  ]
    .filter(Boolean)
    .join(" ");

  return <Component ref={ref} className={classes} {...props} />;
}

export const Text = forwardRef(TextComponent) as <C extends ElementType = "p">(
  props: TextProps<C> & { ref?: PolymorphicRef<C> }
) => ReactElement | null;
```

### 5.3 cn 유틸리티

```typescript
// lib/utils.ts
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

/** Tailwind CSS 클래스 병합 유틸리티 */
export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

---

## 6. Storybook 8 + Chromatic Visual Regression

### 6.1 Storybook 8 설정

```typescript
// .storybook/main.ts
import type { StorybookConfig } from "@storybook/react-vite";

const config: StorybookConfig = {
  stories: ["../src/**/*.mdx", "../src/**/*.stories.@(ts|tsx)"],
  addons: [
    "@storybook/addon-onboarding",
    "@storybook/addon-docs",
    "@storybook/addon-a11y",
    "@storybook/addon-interactions",
    "@storybook/addon-viewport",
    "@storybook/addon-themes",
  ],
  framework: {
    name: "@storybook/react-vite",
    options: {},
  },
  typescript: {
    reactDocgen: "react-docgen-typescript",
    reactDocgenTypescriptOptions: {
      shouldExtractLiteralValuesFromEnum: true,
      shouldRemoveUndefinedFromOptional: true,
      propFilter: (prop) =>
        prop.parent ? !/node_modules/.test(prop.parent.fileName) : true,
    },
  },
  viteFinal: async (config) => {
    // 환경별 토큰 로드
    const env = process.env.STORYBOOK_ENV || "production";
    config.define = {
      ...config.define,
      "process.env.STORYBOOK_ENV": JSON.stringify(env),
    };
    return config;
  },
};

export default config;
```

### 6.2 스토리 작성 표준

```typescript
// components/ui/button.stories.tsx
import type { Meta, StoryObj } from "@storybook/react";
import { expect, within, userEvent } from "@storybook/test";
import { Button } from "./button";

const meta: Meta<typeof Button> = {
  title: "UI/Button",
  component: Button,
  tags: ["autodocs"],
  argTypes: {
    variant: {
      control: "select",
      options: ["primary", "secondary", "destructive", "ghost", "outline", "link"],
    },
    size: {
      control: "select",
      options: ["sm", "md", "lg", "xl", "icon"],
    },
    loading: { control: "boolean" },
    disabled: { control: "boolean" },
  },
};

export default meta;
type Story = StoryObj<typeof meta>;

/** 기본 버튼 */
export const Default: Story = {
  args: {
    children: "Button",
    variant: "primary",
    size: "md",
  },
};

/** 모든 variant/size 조합 */
export const AllVariants: Story = {
  render: () => (
    <div className="flex flex-col gap-4">
      {(["primary", "secondary", "destructive", "ghost", "outline", "link"] as const).map(
        (variant) => (
          <div key={variant} className="flex items-center gap-2">
            <span className="w-24 text-sm text-fg-muted">{variant}</span>
            {(["sm", "md", "lg", "xl"] as const).map((size) => (
              <Button key={size} variant={variant} size={size}>
                {size}
              </Button>
            ))}
          </div>
        )
      )}
    </div>
  ),
};

/** 인터랙션 테스트 */
export const Interactive: Story = {
  args: { children: "Click me" },
  play: async ({ canvasElement }) => {
    const canvas = within(canvasElement);
    const button = canvas.getByRole("button", { name: /click me/i });

    // 포커스 가능 확인
    await userEvent.tab();
    await expect(button).toHaveFocus();

    // 클릭 가능 확인
    await userEvent.click(button);

    // Enter 키 동작 확인
    await userEvent.keyboard("{Enter}");

    // Space 키 동작 확인
    await userEvent.keyboard(" ");
  },
};

/** Playground: 모든 props 조작 */
export const Playground: Story = {
  args: {
    children: "Playground Button",
    variant: "primary",
    size: "md",
    loading: false,
    disabled: false,
  },
};
```

### 6.3 Chromatic 설정

```typescript
// .storybook/chromatic.config.ts
export default {
  /** 스냅샷 대상 뷰포트 */
  viewports: [375, 768, 1280],

  /** 디퓨전 임계값 (0 = 정확한 일치, 0.063 = 기본값) */
  diffThreshold: 0.05,

  /** 특정 스토리 제외 */
  disableSnapshot: false,

  /** 애니메이션 비활성화 (안정적 스냅샷) */
  pauseAnimationAtEnd: true,
};
```

---

## 7. Changesets 버전 관리 + 배포 전략

### 7.1 Changesets 초기 설정

```json
// .changeset/config.json
{
  "$schema": "https://unpkg.com/@changesets/config@3.0.0/schema.json",
  "changelog": [
    "@changesets/changelog-github",
    { "repo": "org/design-system" }
  ],
  "commit": false,
  "fixed": [],
  "linked": [],
  "access": "restricted",
  "baseBranch": "main",
  "updateInternalDependencies": "patch",
  "ignore": [],
  "snapshot": {
    "useCalculatedVersion": true,
    "prereleaseTemplate": "{tag}-{datetime}-{commit}"
  }
}
```

### 7.2 멀티 베타 릴리스 워크플로우

```yaml
# .github/workflows/release.yml
name: Release Design System
on:
  push:
    branches: [main, "beta-*"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - name: Determine release type
        id: release-type
        run: |
          BRANCH="${GITHUB_REF#refs/heads/}"
          if [ "$BRANCH" = "main" ]; then
            echo "type=release" >> "$GITHUB_OUTPUT"
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          elif [[ "$BRANCH" == beta-* ]]; then
            TAG="${BRANCH#beta-}"
            echo "type=snapshot" >> "$GITHUB_OUTPUT"
            echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          fi

      - name: Create Release (stable)
        if: steps.release-type.outputs.type == 'release'
        uses: changesets/action@v1
        with:
          publish: npx changeset publish
          version: npx changeset version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create Snapshot (beta)
        if: steps.release-type.outputs.type == 'snapshot'
        run: |
          npx changeset version --snapshot ${{ steps.release-type.outputs.tag }}
          npx changeset publish --tag ${{ steps.release-type.outputs.tag }}
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
```

### 7.3 Changeset 작성 가이드

```markdown
---
"@org/design-system": minor
---

Button 컴포넌트에 loading prop 추가

- `loading` prop으로 로딩 상태 표시
- 로딩 중 자동 disabled 처리
- 스크린 리더를 위한 aria-busy 설정
```

---

## 8. Tailwind CSS v4 + CSS 변수 통합

### 8.1 Tailwind CSS v4 설정

```css
/* app.css */
@import "tailwindcss";
@import "./tokens/primitive.css";
@import "./tokens/semantic.css";
@import "./tokens/component.css";

/* 디자인 토큰을 Tailwind 테마로 통합 */
@theme {
  --color-bg: var(--color-bg);
  --color-bg-subtle: var(--color-bg-subtle);
  --color-bg-muted: var(--color-bg-muted);

  --color-fg: var(--color-fg);
  --color-fg-muted: var(--color-fg-muted);
  --color-fg-subtle: var(--color-fg-subtle);

  --color-primary: var(--color-primary);
  --color-primary-hover: var(--color-primary-hover);
  --color-primary-fg: var(--color-primary-fg);

  --color-border: var(--color-border);
  --color-ring: var(--color-ring);

  --color-success: var(--color-success);
  --color-warning: var(--color-warning);
  --color-error: var(--color-error);

  --radius-sm: var(--radius-sm);
  --radius-md: var(--radius-md);
  --radius-lg: var(--radius-lg);

  --shadow-sm: var(--shadow-sm);
  --shadow-md: var(--shadow-md);
  --shadow-lg: var(--shadow-lg);
}

/* 기본 스타일 */
@layer base {
  * {
    border-color: var(--color-border);
  }

  body {
    background-color: var(--color-bg);
    color: var(--color-fg);
    font-family: system-ui, sans-serif;
  }

  /* 포커스 표시 기본 스타일 */
  :focus-visible {
    outline: var(--ring-width) solid var(--color-ring);
    outline-offset: var(--ring-offset);
  }

  /* 모션 감소 대응 */
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
      scroll-behavior: auto !important;
    }
  }
}

/* 유틸리티 */
@layer utilities {
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
  }
}
```

### 8.2 환경별 테마 전환 (런타임)

```typescript
// lib/theme.ts
type Theme = "light" | "dark" | "system";

export function applyTheme(theme: Theme): void {
  const root = document.documentElement;

  if (theme === "system") {
    const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
    root.setAttribute("data-theme", prefersDark ? "dark" : "light");

    // 시스템 설정 변경 감지
    window.matchMedia("(prefers-color-scheme: dark)").addEventListener("change", (e) => {
      root.setAttribute("data-theme", e.matches ? "dark" : "light");
    });
  } else {
    root.setAttribute("data-theme", theme);
  }
}

export function getTheme(): Theme {
  return (document.documentElement.getAttribute("data-theme") as Theme) || "system";
}
```

### 8.3 CSS 변수 기반 컴포넌트 커스터마이징

```typescript
// components/ui/card.tsx
import { forwardRef, type ComponentPropsWithoutRef } from "react";
import { cn } from "@/lib/utils";

interface CardProps extends ComponentPropsWithoutRef<"div"> {
  /** CSS 변수로 런타임 커스터마이징 */
  style?: React.CSSProperties & {
    "--card-padding"?: string;
    "--card-radius"?: string;
    "--card-bg"?: string;
    "--card-border"?: string;
    "--card-shadow"?: string;
  };
}

const Card = forwardRef<HTMLDivElement, CardProps>(({ className, ...props }, ref) => (
  <div
    ref={ref}
    className={cn(
      "rounded-[var(--card-radius)] border border-[var(--card-border)]",
      "bg-[var(--card-bg)] p-[var(--card-padding)]",
      "shadow-[var(--card-shadow)]",
      className
    )}
    {...props}
  />
));
Card.displayName = "Card";

const CardHeader = forwardRef<HTMLDivElement, ComponentPropsWithoutRef<"div">>(
  ({ className, ...props }, ref) => (
    <div ref={ref} className={cn("flex flex-col gap-1.5 pb-4", className)} {...props} />
  )
);
CardHeader.displayName = "CardHeader";

const CardTitle = forwardRef<HTMLHeadingElement, ComponentPropsWithoutRef<"h3">>(
  ({ className, ...props }, ref) => (
    <h3 ref={ref} className={cn("text-lg font-semibold leading-none", className)} {...props} />
  )
);
CardTitle.displayName = "CardTitle";

const CardContent = forwardRef<HTMLDivElement, ComponentPropsWithoutRef<"div">>(
  ({ className, ...props }, ref) => (
    <div ref={ref} className={cn("text-sm", className)} {...props} />
  )
);
CardContent.displayName = "CardContent";

export { Card, CardHeader, CardTitle, CardContent };
```

---

## 9. 참고 자료

| 자료 | URL |
|------|-----|
| Radix UI Primitives | https://www.radix-ui.com/primitives |
| shadcn/ui | https://ui.shadcn.com/ |
| Ark UI | https://ark-ui.com/ |
| Tailwind CSS v4 | https://tailwindcss.com/docs |
| class-variance-authority | https://cva.style/ |
| Storybook 8 | https://storybook.js.org/ |
| Chromatic | https://www.chromatic.com/ |
| Changesets | https://github.com/changesets/changesets |
| Design Tokens W3C | https://design-tokens.github.io/community-group/format/ |
| Figma REST API | https://www.figma.com/developers/api |
