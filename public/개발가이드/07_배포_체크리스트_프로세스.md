# 배포 체크리스트 & 프로세스 (2026)

## 목차

1. [AI 기반 배포 자동화](#1-ai-기반-배포-자동화)
   - [1.1 PR Diff 위험도 자동 분석](#11-pr-diff-위험도-자동-분석)
   - [1.2 AI 릴리즈 노트 자동 생성](#12-ai-릴리즈-노트-자동-생성)
   - [1.3 Copilot 배포 스크립트 자동 작성](#13-copilot-배포-스크립트-자동-작성)
   - [1.4 AI 기반 롤백 판단 자동화](#14-ai-기반-롤백-판단-자동화)
   - [1.5 AI 배포 후 이상 탐지](#15-ai-배포-후-이상-탐지)
2. [GitOps 기반 배포](#2-gitops-기반-배포)
3. [Feature Flag 기반 배포](#3-feature-flag-기반-배포)
4. [배포 전략 비교: Canary / Blue-Green / Rolling](#4-배포-전략-비교)
5. [배포 자동화 파이프라인](#5-배포-자동화-파이프라인)
6. [배포 전/중/후 체크리스트](#6-배포-전중후-체크리스트)
7. [모바일 vs 웹 배포 비교](#7-모바일-vs-웹-배포-비교)

---

## 1. AI 기반 배포 자동화

> AI는 배포 파이프라인의 모든 단계에 개입한다.
> PR 분석 -> 릴리즈 노트 생성 -> 배포 스크립트 작성 -> 이상 탐지 -> 자동 롤백까지 End-to-End 자동화가 핵심이다.

### 1.1 PR Diff 위험도 자동 분석

AI 모델이 PR diff, 변경 파일 범위, 과거 장애 이력을 종합해 배포 위험도를 자동 산출한다.

#### 프롬프트 예시 1: 범용 위험도 분석

```text
당신은 시니어 SRE 엔지니어입니다.
아래 PR diff를 분석하여 배포 위험도를 LOW / MEDIUM / HIGH / CRITICAL로 판정하세요.

## 판정 기준
- DB 스키마 변경 또는 마이그레이션 포함 → HIGH 이상
- 인증/결제/개인정보 관련 코드 변경 → HIGH 이상
- API 엔드포인트 삭제 또는 시그니처 변경 → MEDIUM 이상
- 설정 파일(환경변수, feature flag) 변경 → MEDIUM
- 테스트만 변경, 문서만 변경 → LOW
- 동시에 10개 이상 파일 변경 → 1단계 상향

## 출력 형식
1. 위험도: [LOW|MEDIUM|HIGH|CRITICAL]
2. 근거: (3줄 이내)
3. 주의 파일: (최대 5개)
4. 권장 배포 시간대: [업무시간|비업무시간|동결기간]
5. 필요 검토자: [일반|시니어|아키텍트]

## PR Diff:
{diff_content}
```

#### 프롬프트 예시 2: 의존성/영향 범위 분석

```text
당신은 코드 아키텍트입니다.
아래 PR의 변경사항이 시스템 전체에 미치는 영향 범위를 분석하세요.

## 분석 항목
1. 직접 영향 범위: 변경된 모듈/서비스 목록
2. 간접 영향 범위: 변경 모듈을 import/의존하는 다른 모듈
3. API 계약 변경 여부: Request/Response 스키마 변화
4. 하위 호환성: 기존 클라이언트가 영향받는지 여부
5. 필요한 통합 테스트 시나리오: 최소 3개

## 출력 형식 (JSON)
{
  "directImpact": ["서비스A", "서비스B"],
  "indirectImpact": ["서비스C"],
  "breakingChanges": true | false,
  "requiredTests": ["시나리오1", "시나리오2"],
  "deployOrder": ["서비스B → 서비스A → 서비스C"],
  "estimatedRisk": "MEDIUM"
}

## PR Diff:
{diff_content}
```

#### GitHub Actions 위험도 분석 워크플로우

```yaml
# .github/workflows/ai-deploy-risk.yml
name: AI Deploy Risk Analysis

on:
  pull_request:
    types: [opened, synchronize]
    branches: [main, release/*]

jobs:
  risk-analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get PR Diff
        id: diff
        run: |
          DIFF=$(git diff origin/${{ github.base_ref }}...HEAD)
          echo "diff<<EOF" >> $GITHUB_OUTPUT
          echo "$DIFF" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Analyze Risk with Claude
        id: risk
        uses: actions/github-script@v7
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        with:
          script: |
            const Anthropic = require('@anthropic-ai/sdk');
            const client = new Anthropic();

            const diff = `${{ steps.diff.outputs.diff }}`;
            const changedFiles = await exec.getExecOutput('git', [
              'diff', '--name-only', `origin/${context.payload.pull_request.base.ref}...HEAD`
            ]);

            const prompt = `
            시니어 SRE로서 아래 PR diff의 배포 위험도를 분석하세요.

            변경 파일 목록:
            ${changedFiles.stdout}

            Diff 내용:
            ${diff.substring(0, 8000)}

            JSON 형식으로 응답:
            { "risk": "LOW|MEDIUM|HIGH|CRITICAL", "reason": "...", "files": [...], "deployWindow": "..." }
            `;

            const response = await client.messages.create({
              model: 'claude-sonnet-4-20250514',
              max_tokens: 1024,
              messages: [{ role: 'user', content: prompt }]
            });

            const result = JSON.parse(response.content[0].text);
            core.setOutput('risk', result.risk);
            core.setOutput('analysis', JSON.stringify(result));

      - name: Comment Risk Analysis
        uses: actions/github-script@v7
        with:
          script: |
            const analysis = JSON.parse('${{ steps.risk.outputs.analysis }}');
            const emoji = {
              LOW: ':white_check_mark:',
              MEDIUM: ':warning:',
              HIGH: ':rotating_light:',
              CRITICAL: ':no_entry:'
            };

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: `## ${emoji[analysis.risk]} 배포 위험도: **${analysis.risk}**

            **분석 근거:** ${analysis.reason}

            **주의 파일:**
            ${analysis.files.map(f => '- \`' + f + '\`').join('\n')}

            **권장 배포 시간대:** ${analysis.deployWindow}`
            });

      - name: Block CRITICAL deployments
        if: steps.risk.outputs.risk == 'CRITICAL'
        run: |
          echo "::error::CRITICAL 위험도 - 아키텍트 승인 필요"
          exit 1
```

### 1.2 AI 릴리즈 노트 자동 생성

커밋 히스토리와 PR 내용을 기반으로 사용자 친화적 릴리즈 노트를 자동 생성한다.

```yaml
# .github/workflows/ai-release-notes.yml
name: AI Release Notes Generator

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  generate-notes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Collect commits since last tag
        id: commits
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          if [ -z "$PREV_TAG" ]; then
            COMMITS=$(git log --oneline --format="%h %s" HEAD)
          else
            COMMITS=$(git log --oneline --format="%h %s" ${PREV_TAG}..HEAD)
          fi
          echo "commits<<EOF" >> $GITHUB_OUTPUT
          echo "$COMMITS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "prev_tag=$PREV_TAG" >> $GITHUB_OUTPUT

      - name: Generate Release Notes with AI
        id: notes
        uses: actions/github-script@v7
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        with:
          script: |
            const Anthropic = require('@anthropic-ai/sdk');
            const client = new Anthropic();

            const commits = `${{ steps.commits.outputs.commits }}`;

            const response = await client.messages.create({
              model: 'claude-sonnet-4-20250514',
              max_tokens: 2048,
              messages: [{
                role: 'user',
                content: `아래 커밋 목록을 기반으로 릴리즈 노트를 작성하세요.

            커밋 목록:
            ${commits}

            형식:
            ## 새로운 기능
            - ...
            ## 개선사항
            - ...
            ## 버그 수정
            - ...
            ## 기타
            - ...

            규칙:
            - 사용자 관점으로 작성 (코드 내부 용어 사용 금지)
            - 각 항목은 한 줄, 핵심만 간결하게
            - 해당 없는 섹션은 생략`
              }]
            });

            core.setOutput('notes', response.content[0].text);

      - name: Create GitHub Release
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: context.ref.replace('refs/tags/', ''),
              name: `Release ${context.ref.replace('refs/tags/', '')}`,
              body: `${{ steps.notes.outputs.notes }}`,
              draft: false,
              prerelease: false
            });
```

### 1.3 Copilot 배포 스크립트 자동 작성

Copilot/Claude를 활용하여 인프라 설정과 배포 스크립트를 빠르게 생성한다. 아래는 AI에게 요청할 때 효과적인 프롬프트 패턴이다.

```text
## Copilot/Claude 배포 스크립트 생성 프롬프트 패턴

[요청 예시]
"Next.js 앱을 AWS ECS Fargate에 배포하는 GitHub Actions 워크플로우를 작성해줘.
조건:
- 스테이징/프로덕션 환경 분리
- ECR에 Docker 이미지 푸시
- 헬스체크 통과 후 트래픽 전환
- 실패 시 자동 롤백
- Slack 알림 포함"

[핵심 원칙]
- 구체적 기술 스택과 클라우드 환경을 명시한다
- 환경 분리, 롤백, 알림 등 비기능 요구사항을 반드시 포함한다
- 생성된 스크립트는 반드시 팀원이 리뷰한 후 병합한다
```

### 1.4 AI 기반 롤백 판단 자동화

배포 후 메트릭을 실시간 수집하고, AI가 이상 여부를 판단하여 자동 롤백을 트리거한다.

```typescript
// deploy/auto-rollback.ts
interface DeployMetrics {
  errorRate: number;        // 현재 에러율 (%)
  p99Latency: number;       // p99 응답 시간 (ms)
  cpuUsage: number;         // CPU 사용률 (%)
  memoryUsage: number;      // 메모리 사용률 (%)
  healthCheckPass: boolean; // 헬스체크 통과 여부
}

interface RollbackDecision {
  shouldRollback: boolean;
  confidence: number;       // 0~1
  reason: string;
  severity: 'WARNING' | 'CRITICAL';
}

// 정적 규칙 기반 1차 판단 (즉시 롤백)
function evaluateStaticRules(
  current: DeployMetrics,
  baseline: DeployMetrics
): RollbackDecision | null {
  // 헬스체크 실패 → 즉시 롤백
  if (!current.healthCheckPass) {
    return {
      shouldRollback: true,
      confidence: 1.0,
      reason: '헬스체크 연속 실패',
      severity: 'CRITICAL',
    };
  }

  // 에러율 5배 이상 증가 → 즉시 롤백
  if (current.errorRate > baseline.errorRate * 5) {
    return {
      shouldRollback: true,
      confidence: 0.95,
      reason: `에러율 ${current.errorRate.toFixed(1)}% (기준: ${baseline.errorRate.toFixed(1)}%)`,
      severity: 'CRITICAL',
    };
  }

  // p99 레이턴시 3배 이상 증가 → 즉시 롤백
  if (current.p99Latency > baseline.p99Latency * 3) {
    return {
      shouldRollback: true,
      confidence: 0.9,
      reason: `p99 레이턴시 ${current.p99Latency}ms (기준: ${baseline.p99Latency}ms)`,
      severity: 'CRITICAL',
    };
  }

  return null; // 정적 규칙 통과 → AI 분석 진행
}

// AI 기반 2차 판단 (패턴 분석)
async function evaluateWithAI(
  current: DeployMetrics,
  baseline: DeployMetrics,
  recentHistory: DeployMetrics[]
): Promise<RollbackDecision> {
  const prompt = `
배포 후 메트릭을 분석하여 롤백 필요 여부를 판단하세요.

## 기준선 (배포 전 30분 평균)
${JSON.stringify(baseline, null, 2)}

## 현재 (배포 후)
${JSON.stringify(current, null, 2)}

## 최근 5분 추이
${JSON.stringify(recentHistory, null, 2)}

JSON으로 응답: { "shouldRollback": boolean, "confidence": number, "reason": "..." }
  `;

  const response = await callAI(prompt);
  return JSON.parse(response);
}

// 메인 롤백 판단 루프
async function monitorAndRollback(deployId: string): Promise<void> {
  const baseline = await getBaselineMetrics();
  const CHECK_INTERVAL_MS = 30_000;
  const MONITORING_DURATION_MS = 15 * 60_000; // 15분
  const recentHistory: DeployMetrics[] = [];

  const startTime = Date.now();

  while (Date.now() - startTime < MONITORING_DURATION_MS) {
    const current = await getCurrentMetrics();
    recentHistory.push(current);
    if (recentHistory.length > 10) recentHistory.shift();

    // 1차: 정적 규칙 판단
    const staticResult = evaluateStaticRules(current, baseline);
    if (staticResult?.shouldRollback) {
      await executeRollback(deployId, staticResult);
      return;
    }

    // 2차: AI 판단 (5회 수집 후부터)
    if (recentHistory.length >= 5) {
      const aiResult = await evaluateWithAI(current, baseline, recentHistory);
      if (aiResult.shouldRollback && aiResult.confidence > 0.8) {
        await executeRollback(deployId, aiResult);
        return;
      }
    }

    await sleep(CHECK_INTERVAL_MS);
  }

  console.log(`[${deployId}] 모니터링 완료 - 배포 정상`);
}

async function executeRollback(
  deployId: string,
  decision: RollbackDecision
): Promise<void> {
  console.error(`[${deployId}] 롤백 실행: ${decision.reason}`);
  await triggerRollback(deployId);
  await notifySlack({
    channel: '#deploy',
    text: `:rotating_light: 자동 롤백 실행\n배포: ${deployId}\n사유: ${decision.reason}\n확신도: ${(decision.confidence * 100).toFixed(0)}%`,
  });
}
```

### 1.5 AI 배포 후 이상 탐지

에러율 급증, 성능 회귀, 비정상 트래픽 패턴을 AI로 자동 감지한다.

```typescript
// deploy/anomaly-detection.ts
interface AnomalyAlert {
  type: 'ERROR_SPIKE' | 'LATENCY_REGRESSION' | 'TRAFFIC_ANOMALY' | 'MEMORY_LEAK';
  severity: 'LOW' | 'MEDIUM' | 'HIGH' | 'CRITICAL';
  metric: string;
  currentValue: number;
  expectedRange: { min: number; max: number };
  message: string;
}

async function detectAnomalies(deployId: string): Promise<AnomalyAlert[]> {
  const alerts: AnomalyAlert[] = [];
  const metrics = await getPostDeployMetrics(deployId);
  const historical = await getHistoricalMetrics(7); // 최근 7일

  // 에러율 이상 탐지
  const errorBaseline = calculateBaseline(historical.errorRates);
  if (metrics.errorRate > errorBaseline.mean + errorBaseline.stdDev * 3) {
    alerts.push({
      type: 'ERROR_SPIKE',
      severity: metrics.errorRate > errorBaseline.mean * 5 ? 'CRITICAL' : 'HIGH',
      metric: 'error_rate',
      currentValue: metrics.errorRate,
      expectedRange: {
        min: 0,
        max: errorBaseline.mean + errorBaseline.stdDev * 2,
      },
      message: `에러율 ${metrics.errorRate.toFixed(2)}% (정상 범위: ~${(errorBaseline.mean + errorBaseline.stdDev * 2).toFixed(2)}%)`,
    });
  }

  // 메모리 누수 탐지 (선형 증가 패턴)
  const memoryTrend = calculateLinearRegression(metrics.memoryTimeSeries);
  if (memoryTrend.slope > 0.5 && memoryTrend.rSquared > 0.85) {
    alerts.push({
      type: 'MEMORY_LEAK',
      severity: 'HIGH',
      metric: 'memory_usage',
      currentValue: metrics.memoryUsage,
      expectedRange: { min: 0, max: 80 },
      message: `메모리 사용량 선형 증가 감지 (기울기: ${memoryTrend.slope.toFixed(2)}%/min)`,
    });
  }

  // AI 기반 종합 이상 탐지
  if (alerts.length > 0) {
    const aiAnalysis = await analyzeWithAI(metrics, historical, alerts);
    // AI가 오탐으로 판단한 알림 제거
    return alerts.filter(
      (alert) => !aiAnalysis.falsePositives.includes(alert.type)
    );
  }

  return alerts;
}

function calculateBaseline(values: number[]): { mean: number; stdDev: number } {
  const mean = values.reduce((a, b) => a + b, 0) / values.length;
  const variance =
    values.reduce((sum, val) => sum + Math.pow(val - mean, 2), 0) / values.length;
  return { mean, stdDev: Math.sqrt(variance) };
}
```

---

## 2. GitOps 기반 배포

Git 저장소를 단일 진실 공급원(Single Source of Truth)으로 사용하여, 선언적으로 인프라와 배포를 관리한다.

### 환경별 디렉토리 구조

```
deploy/
├── base/                          # 공통 리소스
│   ├── deployment.yaml
│   ├── service.yaml
│   ├── hpa.yaml
│   └── kustomization.yaml
├── overlays/
│   ├── development/
│   │   ├── kustomization.yaml     # 개발 환경 오버라이드
│   │   ├── replicas-patch.yaml
│   │   └── env-config.yaml
│   ├── staging/
│   │   ├── kustomization.yaml
│   │   ├── replicas-patch.yaml
│   │   └── env-config.yaml
│   └── production/
│       ├── kustomization.yaml
│       ├── replicas-patch.yaml
│       ├── env-config.yaml
│       └── resource-limits-patch.yaml
└── argocd/
    └── application.yaml
```

### ArgoCD Application 설정

```yaml
# deploy/argocd/application.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/org/deploy-config.git
    targetRevision: main
    path: deploy/overlays/production
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    automated:
      prune: true          # Git에서 삭제된 리소스 자동 제거
      selfHeal: true       # 수동 변경 자동 복원
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
```

### Flux Kustomization 설정

```yaml
# deploy/flux/kustomization.yaml
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: my-service
  namespace: flux-system
spec:
  interval: 5m
  path: ./deploy/overlays/production
  prune: true
  sourceRef:
    kind: GitRepository
    name: deploy-config
  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: my-service
      namespace: production
  timeout: 3m
  retryInterval: 1m
```

---

## 3. Feature Flag 기반 배포

코드 배포와 기능 출시를 분리한다. 코드는 main에 머지하되, Feature Flag로 활성화 시점을 제어한다.

### LaunchDarkly 연동

```typescript
// features/launchdarkly-client.ts
import * as LaunchDarkly from 'launchdarkly-node-server-sdk';

const ldClient = LaunchDarkly.init(process.env.LAUNCHDARKLY_SDK_KEY!);

export async function isFeatureEnabled(
  flagKey: string,
  userId: string,
  attributes?: Record<string, unknown>
): Promise<boolean> {
  await ldClient.waitForInitialization();

  const context: LaunchDarkly.LDContext = {
    kind: 'user',
    key: userId,
    ...attributes,
  };

  return ldClient.variation(flagKey, context, false);
}
```

### Unleash 연동

```typescript
// features/unleash-client.ts
import { initialize, isEnabled } from 'unleash-client';

const unleash = initialize({
  url: process.env.UNLEASH_API_URL!,
  appName: 'my-service',
  customHeaders: {
    Authorization: process.env.UNLEASH_API_TOKEN!,
  },
});

export function checkFeature(
  featureName: string,
  context?: { userId?: string; properties?: Record<string, string> }
): boolean {
  return isEnabled(featureName, context);
}
```

### 자체 구현: 해싱 기반 점진적 롤아웃

```typescript
// features/gradual-rollout.ts
import { createHash } from 'crypto';

interface RolloutConfig {
  featureKey: string;
  percentage: number;    // 0~100
  allowList?: string[];  // 무조건 활성화할 사용자
  blockList?: string[];  // 무조건 비활성화할 사용자
}

/**
 * 해시 기반 일관적 롤아웃.
 * 동일 userId + featureKey 조합은 항상 같은 결과를 반환한다.
 */
export function isRolledOut(config: RolloutConfig, userId: string): boolean {
  // 명시적 목록 우선
  if (config.allowList?.includes(userId)) return true;
  if (config.blockList?.includes(userId)) return false;

  // 해시 기반 결정
  const hash = createHash('sha256')
    .update(`${config.featureKey}:${userId}`)
    .digest('hex');
  const bucket = parseInt(hash.substring(0, 8), 16) % 100;

  return bucket < config.percentage;
}

// 사용 예시
const newCheckoutConfig: RolloutConfig = {
  featureKey: 'new-checkout-flow',
  percentage: 20,       // 20% 사용자에게 활성화
  allowList: ['beta-tester-1', 'qa-team'],
};

const enabled = isRolledOut(newCheckoutConfig, currentUserId);
```

### React Context/Hook 패턴

```typescript
// features/FeatureFlagProvider.tsx
import { createContext, useContext, useEffect, useState, type ReactNode } from 'react';

interface FeatureFlags {
  [key: string]: boolean;
}

interface FeatureFlagContextValue {
  flags: FeatureFlags;
  isEnabled: (key: string) => boolean;
  loading: boolean;
}

const FeatureFlagContext = createContext<FeatureFlagContextValue>({
  flags: {},
  isEnabled: () => false,
  loading: true,
});

export function FeatureFlagProvider({ children }: { children: ReactNode }) {
  const [flags, setFlags] = useState<FeatureFlags>({});
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    async function fetchFlags() {
      try {
        const response = await fetch('/api/features');
        const data = await response.json();
        setFlags(data.flags);
      } catch (error) {
        console.error('Feature flag 로드 실패:', error);
      } finally {
        setLoading(false);
      }
    }

    fetchFlags();

    // SSE로 실시간 업데이트 수신
    const eventSource = new EventSource('/api/features/stream');
    eventSource.onmessage = (event) => {
      const updated = JSON.parse(event.data);
      setFlags((prev) => ({ ...prev, ...updated }));
    };

    return () => eventSource.close();
  }, []);

  const isEnabled = (key: string): boolean => flags[key] ?? false;

  return (
    <FeatureFlagContext.Provider value={{ flags, isEnabled, loading }}>
      {children}
    </FeatureFlagContext.Provider>
  );
}

export function useFeatureFlag(key: string): boolean {
  const { isEnabled } = useContext(FeatureFlagContext);
  return isEnabled(key);
}

// 사용 예시
function CheckoutPage() {
  const useNewFlow = useFeatureFlag('new-checkout-flow');

  return useNewFlow ? <NewCheckoutFlow /> : <LegacyCheckoutFlow />;
}
```

---

## 4. 배포 전략 비교

### 전략별 비교표

| 항목 | Canary | Blue-Green | Rolling |
|------|--------|------------|---------|
| **개념** | 소수 트래픽으로 새 버전 검증 후 점진 확대 | 두 환경을 전환하여 즉시 교체 | 인스턴스를 순차적으로 교체 |
| **롤백 속도** | 빠름 (트래픽 비율 조정) | 즉시 (환경 전환) | 느림 (순차 복원) |
| **인프라 비용** | 중간 | 높음 (2배 리소스) | 낮음 |
| **위험도** | 낮음 | 매우 낮음 | 중간 |
| **복잡도** | 높음 (트래픽 분배 필요) | 중간 | 낮음 |
| **무중단** | O | O | O (maxUnavailable 설정) |
| **적합 상황** | 대규모 사용자, 위험 변경 | 빠른 롤백 필요, 충분한 인프라 | 일반적 업데이트, 비용 제한 |

### Canary 배포 (Kubernetes + Istio)

```yaml
# deploy/canary/virtual-service.yaml
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: my-service
spec:
  hosts:
    - my-service
  http:
    - route:
        - destination:
            host: my-service
            subset: stable
          weight: 90
        - destination:
            host: my-service
            subset: canary
          weight: 10
---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: my-service
spec:
  host: my-service
  subsets:
    - name: stable
      labels:
        version: v1
    - name: canary
      labels:
        version: v2
```

### Blue-Green 배포 (Kubernetes Service 전환)

```yaml
# deploy/blue-green/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  selector:
    app: my-service
    # 전환 시 blue ↔ green 변경
    slot: green
  ports:
    - port: 80
      targetPort: 8080
---
# Blue Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-service-blue
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-service
      slot: blue
  template:
    metadata:
      labels:
        app: my-service
        slot: blue
    spec:
      containers:
        - name: app
          image: my-registry/my-service:v1.0.0
          ports:
            - containerPort: 8080
---
# Green Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-service-green
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-service
      slot: green
  template:
    metadata:
      labels:
        app: my-service
        slot: green
    spec:
      containers:
        - name: app
          image: my-registry/my-service:v2.0.0
          ports:
            - containerPort: 8080
```

### Rolling 배포 (Kubernetes 기본)

```yaml
# deploy/rolling/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-service
spec:
  replicas: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1          # 동시에 추가 생성 가능한 Pod 수
      maxUnavailable: 0     # 최소 가용 Pod 보장 (무중단)
  selector:
    matchLabels:
      app: my-service
  template:
    metadata:
      labels:
        app: my-service
    spec:
      containers:
        - name: app
          image: my-registry/my-service:v2.0.0
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
```

---

## 5. 배포 자동화 파이프라인

### GitHub Actions 3단계 파이프라인: 빌드 -> 스테이징 -> 프로덕션

```yaml
# .github/workflows/deploy-pipeline.yml
name: Deploy Pipeline

on:
  push:
    branches: [main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false      # 배포는 취소하지 않음

env:
  IMAGE_REGISTRY: ghcr.io/${{ github.repository }}

jobs:
  # ──────────────────────────────
  # Stage 1: 빌드 & 테스트
  # ──────────────────────────────
  build:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - run: npm ci
      - run: npm run lint
      - run: npm run type-check
      - run: npm test -- --coverage

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_REGISTRY }}
          tags: |
            type=sha,prefix={{branch}}-

      - name: Build & Push Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ──────────────────────────────
  # Stage 2: 스테이징 배포 & 검증
  # ──────────────────────────────
  staging:
    needs: build
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Staging
        run: |
          kubectl set image deployment/my-service \
            app=${{ env.IMAGE_REGISTRY }}:${{ needs.build.outputs.image_tag }} \
            -n staging
          kubectl rollout status deployment/my-service -n staging --timeout=300s

      - name: Run Smoke Tests
        run: |
          npm run test:e2e -- --config baseUrl=https://staging.example.com

      - name: Staging Health Check
        run: |
          for i in $(seq 1 10); do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://staging.example.com/health)
            if [ "$STATUS" = "200" ]; then
              echo "Health check passed"
              exit 0
            fi
            sleep 5
          done
          echo "Health check failed"
          exit 1

  # ──────────────────────────────
  # Stage 3: 프로덕션 배포
  # ──────────────────────────────
  production:
    needs: [build, staging]
    runs-on: ubuntu-latest
    environment: production       # 수동 승인 게이트 가능
    steps:
      - uses: actions/checkout@v4

      - name: Record deploy start
        id: deploy-start
        run: echo "start_time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: Deploy to Production
        run: |
          kubectl set image deployment/my-service \
            app=${{ env.IMAGE_REGISTRY }}:${{ needs.build.outputs.image_tag }} \
            -n production
          kubectl rollout status deployment/my-service -n production --timeout=600s

      - name: Post-deploy Validation
        id: validate
        run: |
          sleep 30
          ERROR_RATE=$(curl -s "https://prometheus.example.com/api/v1/query?query=rate(http_errors_total[5m])" | jq '.data.result[0].value[1]' -r)
          if (( $(echo "$ERROR_RATE > 0.05" | bc -l) )); then
            echo "error_rate_high=true" >> $GITHUB_OUTPUT
          else
            echo "error_rate_high=false" >> $GITHUB_OUTPUT
          fi

      - name: Auto Rollback on Failure
        if: steps.validate.outputs.error_rate_high == 'true'
        run: |
          echo "::error::에러율 초과 - 자동 롤백 실행"
          kubectl rollout undo deployment/my-service -n production
          kubectl rollout status deployment/my-service -n production --timeout=300s

      - name: Notify Slack - Success
        if: success() && steps.validate.outputs.error_rate_high != 'true'
        uses: slackapi/slack-github-action@v1.27.0
        with:
          payload: |
            {
              "channel": "#deploy",
              "text": ":white_check_mark: *프로덕션 배포 성공*\n이미지: `${{ needs.build.outputs.image_tag }}`\n커밋: `${{ github.sha }}`\n실행자: ${{ github.actor }}"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Notify Slack - Rollback
        if: steps.validate.outputs.error_rate_high == 'true'
        uses: slackapi/slack-github-action@v1.27.0
        with:
          payload: |
            {
              "channel": "#deploy",
              "text": ":rotating_light: *프로덕션 자동 롤백 실행*\n이미지: `${{ needs.build.outputs.image_tag }}`\n사유: 에러율 임계치 초과\n실행자: ${{ github.actor }}"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Notify Slack - Failure
        if: failure()
        uses: slackapi/slack-github-action@v1.27.0
        with:
          payload: |
            {
              "channel": "#deploy",
              "text": ":x: *프로덕션 배포 실패*\n이미지: `${{ needs.build.outputs.image_tag }}`\n실행자: ${{ github.actor }}\n로그: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
```

---

## 6. 배포 전/중/후 체크리스트

### 배포 전 (Pre-deploy)

| 카테고리 | 체크 항목 | 담당 |
|----------|----------|------|
| **코드 품질** | PR 리뷰 완료 및 승인 | 개발자 |
| | 모든 CI 테스트 통과 (unit, integration, e2e) | CI |
| | lint / type-check 통과 | CI |
| | AI 위험도 분석 결과 확인 | AI + 개발자 |
| **빌드** | 빌드 성공 및 아티팩트 생성 확인 | CI |
| | Docker 이미지 취약점 스캔 통과 | CI |
| | 번들 사이즈 임계치 이내 | CI |
| **환경** | 환경변수 / Secret 변경사항 반영 | DevOps |
| | DB 마이그레이션 스크립트 검증 완료 | DBA / 개발자 |
| | Feature Flag 설정 확인 | 개발자 |
| **의존성** | 외부 서비스 의존성 확인 (API 버전, 가용성) | 개발자 |
| | 하위 호환성 보장 여부 확인 | 개발자 |
| **커뮤니케이션** | 배포 일정 팀 공유 | 배포 담당자 |
| | 관련 팀 (CS, QA) 사전 공유 | 배포 담당자 |

### 배포 중 (During deploy)

| 카테고리 | 체크 항목 | 담당 |
|----------|----------|------|
| **배포 실행** | 배포 시작 Slack 알림 발송 | CI |
| | 배포 순서 준수 (DB -> Backend -> Frontend) | DevOps |
| | 롤아웃 상태 실시간 모니터링 | 배포 담당자 |
| **헬스체크** | 새 인스턴스 헬스체크 통과 확인 | CI |
| | readiness / liveness probe 정상 | K8s |
| **트래픽** | Canary 배포 시 트래픽 비율 단계적 증가 | DevOps |
| | 에러율 실시간 모니터링 | 배포 담당자 |
| **롤백 준비** | 이전 버전 이미지 즉시 롤백 가능 상태 확인 | DevOps |
| | 롤백 명령어 / 버튼 준비 | 배포 담당자 |

### 배포 후 (Post-deploy)

| 카테고리 | 체크 항목 | 담당 |
|----------|----------|------|
| **검증** | Smoke 테스트 통과 | QA / CI |
| | 핵심 사용자 시나리오 수동 확인 | QA |
| | AI 이상 탐지 결과 확인 (15분간) | AI + 개발자 |
| **메트릭** | 에러율 기준선 대비 정상 범위 | 모니터링 |
| | p99 레이턴시 기준선 대비 정상 범위 | 모니터링 |
| | CPU / 메모리 사용량 안정적 | 모니터링 |
| **후속 작업** | 배포 완료 Slack 알림 발송 | CI |
| | 릴리즈 노트 발행 (AI 자동 생성) | CI |
| | Feature Flag 점진적 활성화 시작 | 개발자 |
| | 관련 Jira 티켓 상태 업데이트 | 개발자 |
| **관찰** | 배포 후 1시간 집중 모니터링 | 배포 담당자 |
| | 24시간 내 주요 메트릭 추이 확인 | 배포 담당자 |
| | 이상 발견 시 즉시 롤백 또는 핫픽스 | 개발자 |

---

## 7. 모바일 vs 웹 배포 비교

| 항목 | 웹 배포 | 모바일 앱 배포 |
|------|---------|---------------|
| **배포 속도** | 즉시 (서버 배포 완료 시점) | 스토어 심사 1~7일 (Apple 기준) |
| **롤백** | 즉시 가능 (이전 버전 배포) | 불가능 (새 버전 제출 필요, 긴급 시 서버 사이드 제어) |
| **강제 업데이트** | 자동 (사용자 개입 불필요) | 불가 (사용자가 업데이트 수락 필요, 강제 업데이트 팝업 가능) |
| **버전 파편화** | 없음 (단일 버전) | 심함 (구 버전 사용자 상존, API 하위 호환 필수) |
| **Feature Flag 중요도** | 높음 | 매우 높음 (롤백 불가하므로 필수) |
| **테스트 범위** | 브라우저 호환성 | OS 버전 + 기기 다양성 + 스토어 정책 준수 |
| **배포 파이프라인** | CI/CD 자동화 (GitHub Actions 등) | CI + Fastlane + TestFlight/Internal Testing → 스토어 제출 |
| **핫픽스** | 수 분 내 배포 가능 | 스토어 긴급 심사 요청 (수 시간~1일), CodePush 등 OTA 활용 |
| **환경 설정** | 서버 환경변수로 즉시 변경 | 앱 번들 내 포함 또는 Remote Config으로 변경 |
| **모니터링** | APM, 서버 로그 | Crashlytics, 앱 성능 모니터링, 스토어 리뷰 모니터링 |
| **A/B 테스트** | 서버 사이드 즉시 적용 | Firebase Remote Config / 스토어 단계적 출시 (Staged Rollout) |
| **보안 업데이트** | 서버 측 즉시 패치 | 클라이언트 업데이트 필요 (인증서 피닝, 암호화 등) |
