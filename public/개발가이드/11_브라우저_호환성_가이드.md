# 브라우저 호환성 가이드 -- AI 활용 극대화 + 멀티 베타 + 2026 최신 트렌드

## 목차

1. [AI 프롬프트 5선](#1-ai-프롬프트-5선)
2. [Web Platform Baseline 2026](#2-web-platform-baseline-2026)
3. [CSS 2026: Anchor Positioning / Popover / @scope](#3-css-2026-anchor-positioning--popover--scope)
4. [멀티 베타 + Preview별 BrowserStack 매트릭스](#4-멀티-베타--preview별-browserstack-매트릭스)
5. [Import Maps](#5-import-maps)
6. [UA Client Hints](#6-ua-client-hints)
7. [환경별 Polyfill 전략](#7-환경별-polyfill-전략)
8. [Playwright 크로스 브라우저 자동화](#8-playwright-크로스-브라우저-자동화)
9. [AI 기반 호환성 이슈 자동 탐지](#9-ai-기반-호환성-이슈-자동-탐지)
10. [Preview 환경 호환성 자동 검증 CI](#10-preview-환경-호환성-자동-검증-ci)
11. [체크리스트](#11-체크리스트)
12. [참고 자료](#12-참고-자료)

---

## 1. AI 프롬프트 5선

### 프롬프트 1: Safari CSS 디버깅

> ```
> Safari 18.x에서 아래 CSS가 의도대로 렌더링되지 않는다.
> (1) Safari WebKit 엔진의 알려진 버그/미구현 속성을 식별하고,
> (2) Safari Web Inspector에서 확인할 디버깅 단계를 나열하고,
> (3) @supports를 활용한 Safari 전용 Fallback CSS를 작성하고,
> (4) WebKit Bugzilla에 관련 이슈가 있는지 URL을 찾아줘.
>
> --- 문제 CSS ---
> {CSS 코드 붙여넣기}
>
> --- 증상 ---
> {스크린샷 설명 또는 예상 vs 실제 렌더링 차이}
>
> --- 환경 ---
> Safari 버전: {버전}
> macOS/iOS 버전: {버전}
> 재현 빈도: {항상/간헐적}
> ```

### 프롬프트 2: API 지원 확인

> ```
> 다음 Web API 목록의 브라우저 지원 현황을 분석해줘.
> 각 API에 대해:
> (1) Web Platform Baseline 상태 (Widely Available / Newly Available / Limited)
> (2) 미지원 브라우저 + 최소 지원 버전 (Chrome, Firefox, Safari, Edge, Samsung Internet)
> (3) Feature Detection 코드 (TypeScript, 런타임 안전)
> (4) caniuse 글로벌 커버리지 %
> (5) 대체 API가 있으면 마이그레이션 코드
>
> --- API 목록 ---
> {API 이름 나열}
>
> --- 타겟 browserslist ---
> last 2 Chrome versions, last 2 Firefox versions,
> last 2 Safari versions, last 2 Edge versions,
> last 2 iOS versions, last 2 Samsung versions
> ```

### 프롬프트 3: Polyfill 생성

> ```
> 다음 API들에 대해 프로덕션 Polyfill 코드를 TypeScript로 생성해줘.
> 각 Polyfill은 반드시:
> (1) Feature Detection으로 네이티브 지원 시 완전히 스킵 (번들에서 제거 가능)
> (2) TC39/W3C 스펙 준수 + 엣지 케이스(null, Symbol, Proxy 등) 처리
> (3) ESM 구조로 트리셰이킹 가능하게 작성
> (4) 조건부 동적 import() 래퍼 포함 (네트워크 비용 최소화)
> (5) 번들 크기 예상치(gzip 기준)를 주석으로 표기
>
> --- 대상 API ---
> {API 목록 붙여넣기}
>
> --- browserslist ---
> {browserslist 쿼리 또는 npx browserslist 출력 붙여넣기}
> ```

### 프롬프트 4: 테스트 결과 분석

> ```
> 다음은 멀티 베타 환경(production, beta-canary, pr-456)에서 실행한
> Playwright 크로스 브라우저 테스트 결과(JSON)이다.
> (1) 브라우저별 실패 원인 분석 (호환성 이슈 vs 로직 버그 vs 환경 차이로 분류)
> (2) 동일 코드인데 환경마다 결과가 다른 테스트 식별 + 원인 추정
> (3) 각 실패에 대한 수정 코드 (TypeScript)
> (4) 사용자 영향 범위 기반 우선순위 (P0~P3)
> (5) 회귀 가능성이 있는 테스트에 대해 추가 assertion 제안
>
> --- Playwright JSON 보고서 ---
> {JSON 붙여넣기}
>
> --- 환경 메타데이터 ---
> Production: {배포 버전/커밋}
> Beta: {배포 버전/커밋}
> PR Preview: {PR 번호, 변경 파일 목록}
> ```

### 프롬프트 5: 지원 범위 추천

> ```
> 다음 프로젝트의 실제 사용자 브라우저 분포 데이터를 기반으로:
> (1) 최적의 browserslist 쿼리 추천 (커버리지 98%+ 목표)
> (2) 현재 대비 커버리지 변화를 % + 실 사용자 수로 환산
> (3) 지원 범위 변경에 따른 번들 크기 영향 예측 (core-js, CSS prefix 기준)
> (4) Tier 1 / Tier 2 / Tier 3 지원 등급 분류 제안
> (5) 멀티 베타 환경별 차등 지원 전략 (Preview는 Tier 1만 등)
> (6) 6개월 뒤 트렌드 예측 기반 선제적 드롭 후보 브라우저
>
> --- 브라우저 분포 (최근 90일) ---
> {Google Analytics / 자체 수집 데이터 붙여넣기}
>
> --- 현재 browserslist ---
> {현재 설정 붙여넣기}
>
> --- 프로젝트 특성 ---
> {B2B/B2C, 모바일 비중, 지역, 핵심 기능에 사용하는 Web API 등}
> ```

---

## 2. Web Platform Baseline 2026

### 2.1 권장 지원 범위

**Desktop**

| 브라우저 | 최소 버전 | 비고 |
|---------|----------|------|
| Chrome | 134+ | 최신 2 메이저 |
| Firefox | 136+ | 최신 2 메이저 |
| Safari | 18.2+ | 최신 2 메이저 |
| Edge | 134+ | Chromium 기반, Chrome과 동일 |

**Mobile**

| 브라우저 | 최소 버전 | 비고 |
|---------|----------|------|
| iOS Safari | 18.2+ | iPadOS 포함 |
| Chrome Android | 최신 | 자동 업데이트 |
| Samsung Internet | 27+ | Chromium 134 기반 |

### 2.2 browserslist 설정

```
# .browserslistrc
last 2 Chrome versions
last 2 Firefox versions
last 2 Safari versions
last 2 Edge versions
last 2 iOS versions
last 2 ChromeAndroid versions
last 2 Samsung versions
not dead
```

### 2.3 지원 등급 (Tiered Support)

| 등급 | 대상 브라우저 | 지원 수준 |
|------|-------------|----------|
| Tier 1 (완전 지원) | Chrome, Edge, Firefox, Safari 최신 2버전 | 모든 기능 정상 동작, 버그 즉시 수정 |
| Tier 2 (기본 지원) | Samsung Internet, iOS Chrome, Firefox ESR | 핵심 기능 정상, 시각적 차이 허용 |
| Tier 3 (최소 지원) | 그 외 모던 브라우저 | 콘텐츠 접근 가능, 고급 기능 Graceful Degradation |

### 2.4 Baseline 기능 분류

#### Widely Available (Polyfill 없이 안전 사용)

| 기능 | Baseline 연도 | 비고 |
|------|-------------|------|
| CSS Grid | 2020 | `display: grid`, `grid-template`, `gap` |
| CSS Custom Properties | 2020 | `--var`, `var()` |
| ES Modules | 2020 | `import` / `export` |
| IntersectionObserver | 2020 | Lazy Load, 무한 스크롤 |
| CSS `aspect-ratio` | 2022 | 비율 유지 레이아웃 |
| `structuredClone()` | 2022 | 깊은 복사 |
| CSS Container Queries | 2023 | `@container`, `container-type` |
| CSS Subgrid | 2023 | 중첩 그리드 정렬 |
| CSS `:has()` Selector | 2024 | 부모/형제 기반 선택 |
| CSS Nesting | 2024 | 네이티브 `&` 중첩 |
| Cascade Layers | 2024 | `@layer` 기반 우선순위 |
| View Transitions API (SPA) | 2024 | `document.startViewTransition()` |

#### Newly Available (Feature Detection 필수)

| 기능 | Baseline 연도 | 비고 |
|------|-------------|------|
| Import Maps | 2023 | 번들러 없는 모듈 매핑 |
| Popover API | 2025 | `popover` 속성, `showPopover()` |
| CSS Anchor Positioning | 2025 | `anchor()`, `position-anchor` |
| CSS `@scope` | 2025 | 범위 지정 스타일링 |
| `Set` 메서드 (`union`, `intersection`) | 2025 | 집합 연산 |
| `Promise.withResolvers()` | 2025 | 외부에서 resolve/reject 접근 |
| `Object.groupBy()` | 2025 | 배열 그룹핑 |

#### Limited Availability (주의 -- Fallback 필수)

| 기능 | 상태 | 비고 |
|------|------|------|
| View Transitions API (MPA) | Chrome/Edge만 | `@view-transition` at-rule |
| CSS `text-wrap: pretty` | Chrome/Edge만 | 단락 마지막 줄 최적화 |
| Speculation Rules API | Chrome/Edge만 | 선제적 프리렌더링 |

### 2.5 Feature Detection 유틸

```typescript
// utils/baseline-detect.ts

const BASELINE_CHECKS = {
  containerQueries: () => CSS.supports("container-type", "inline-size"),
  viewTransitions: () => "startViewTransition" in document,
  importMaps: () => HTMLScriptElement.supports?.("importmap") ?? false,
  cssNesting: () => CSS.supports("selector(&)"),
  cascadeLayers: () => CSS.supports("@layer base"),
  subgrid: () => CSS.supports("grid-template-rows", "subgrid"),
  hasSelector: () => CSS.supports("selector(:has(*))"),
  popover: () => "popover" in HTMLElement.prototype,
  anchorPositioning: () => CSS.supports("position-anchor", "--any"),
  cssScope: () => CSS.supports("selector(:scope)") && CSS.supports("@scope (div) { }".length > 0 ? "@scope" : ""),
  setMethods: () => typeof Set.prototype.union === "function",
  objectGroupBy: () => typeof Object.groupBy === "function",
  promiseWithResolvers: () => typeof Promise.withResolvers === "function",
} as const;

type BaselineFeature = keyof typeof BASELINE_CHECKS;

interface BaselineReport {
  supported: BaselineFeature[];
  unsupported: BaselineFeature[];
  details: Record<BaselineFeature, boolean>;
}

function checkBaseline(features?: BaselineFeature[]): BaselineReport {
  const targets = features ?? (Object.keys(BASELINE_CHECKS) as BaselineFeature[]);
  const details = {} as Record<BaselineFeature, boolean>;

  for (const feature of targets) {
    try {
      details[feature] = BASELINE_CHECKS[feature]();
    } catch {
      details[feature] = false;
    }
  }

  return {
    supported: targets.filter((f) => details[f]),
    unsupported: targets.filter((f) => !details[f]),
    details,
  };
}

/** 런타임에 호출하여 콘솔에 지원 현황을 출력한다 */
function logBaselineReport(): void {
  const report = checkBaseline();
  console.group("[Baseline 2026] 브라우저 기능 지원 현황");
  for (const [feature, supported] of Object.entries(report.details)) {
    console.log(`${supported ? "  OK" : "  NG"} ${feature}`);
  }
  console.groupEnd();
}

export { checkBaseline, logBaselineReport, BASELINE_CHECKS };
export type { BaselineFeature, BaselineReport };
```

---

## 3. CSS 2026: Anchor Positioning / Popover / @scope

### 3.1 CSS Anchor Positioning

요소를 다른 요소에 상대적으로 위치시키는 네이티브 CSS 기능이다. JavaScript 기반 포지셔닝 라이브러리(Floating UI 등)를 대체한다.

```css
/* 앵커 요소 등록 */
.trigger-button {
  anchor-name: --menu-anchor;
}

/* 앵커 대상 위치 지정 */
.dropdown-menu {
  position: fixed;
  position-anchor: --menu-anchor;

  /* 앵커 아래쪽, 왼쪽 정렬 */
  top: anchor(bottom);
  left: anchor(left);

  /* 뷰포트를 벗어날 때 반대쪽으로 전환 */
  position-try-fallbacks: flip-block, flip-inline;

  /* 크기 제한: 앵커와 뷰포트 가장자리 사이 */
  max-height: calc(anchor(--menu-anchor, bottom, 100vh) - 16px);
}

/* Fallback: Anchor Positioning 미지원 브라우저 */
@supports not (position-anchor: --any) {
  .dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    max-height: 300px;
  }
}
```

```typescript
// components/AnchoredPopup.ts -- Feature Detection 기반 분기

interface PopupPosition {
  top: number;
  left: number;
  maxHeight: number;
}

function supportsAnchorPositioning(): boolean {
  return CSS.supports("position-anchor", "--test");
}

/**
 * Anchor Positioning 미지원 시 JavaScript Fallback으로 위치를 계산한다.
 * 지원 브라우저에서는 CSS가 처리하므로 호출하지 않는다.
 */
function calculateFallbackPosition(
  anchor: HTMLElement,
  popup: HTMLElement,
): PopupPosition {
  const anchorRect = anchor.getBoundingClientRect();
  const viewportHeight = window.innerHeight;

  const spaceBelow = viewportHeight - anchorRect.bottom;
  const spaceAbove = anchorRect.top;

  const placeBelow = spaceBelow >= 200 || spaceBelow >= spaceAbove;

  return {
    top: placeBelow ? anchorRect.bottom + 4 : anchorRect.top - popup.offsetHeight - 4,
    left: anchorRect.left,
    maxHeight: (placeBelow ? spaceBelow : spaceAbove) - 16,
  };
}

function initAnchoredPopup(
  anchorSelector: string,
  popupSelector: string,
): void {
  if (supportsAnchorPositioning()) {
    // CSS가 모든 위치 계산을 처리한다. JS에서는 아무것도 하지 않는다.
    return;
  }

  const anchor = document.querySelector<HTMLElement>(anchorSelector);
  const popup = document.querySelector<HTMLElement>(popupSelector);
  if (!anchor || !popup) return;

  function updatePosition(): void {
    const pos = calculateFallbackPosition(anchor!, popup!);
    popup!.style.position = "fixed";
    popup!.style.top = `${pos.top}px`;
    popup!.style.left = `${pos.left}px`;
    popup!.style.maxHeight = `${pos.maxHeight}px`;
  }

  anchor.addEventListener("click", updatePosition);
  window.addEventListener("resize", updatePosition);
  window.addEventListener("scroll", updatePosition, { passive: true });
}

export { initAnchoredPopup, supportsAnchorPositioning, calculateFallbackPosition };
```

### 3.2 Popover API

모달/툴팁/드롭다운을 네이티브로 처리한다. Light Dismiss, Top Layer 자동 배치, 접근성이 내장되어 있다.

```css
/* Popover 기본 스타일 */
[popover] {
  margin: 0;
  padding: 1rem;
  border: 1px solid var(--color-border, #e5e7eb);
  border-radius: 8px;
  box-shadow: 0 4px 24px rgb(0 0 0 / 0.12);

  /* 열기/닫기 전환 애니메이션 */
  opacity: 0;
  transform: translateY(-8px) scale(0.96);
  transition:
    opacity 200ms ease,
    transform 200ms ease,
    overlay 200ms ease allow-discrete,
    display 200ms ease allow-discrete;

  &:popover-open {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  /* 닫힐 때 시작점 */
  @starting-style {
    &:popover-open {
      opacity: 0;
      transform: translateY(-8px) scale(0.96);
    }
  }
}

/* Popover 백드롭 */
[popover]::backdrop {
  background: rgb(0 0 0 / 0.2);
  opacity: 0;
  transition: opacity 200ms ease, display 200ms ease allow-discrete;
}

[popover]:popover-open::backdrop {
  opacity: 1;
}
```

```html
<!-- 선언적 Popover: JavaScript 불필요 -->
<button popovertarget="menu-popover">메뉴 열기</button>

<div id="menu-popover" popover>
  <nav>
    <a href="/dashboard">대시보드</a>
    <a href="/settings">설정</a>
  </nav>
</div>

<!-- 수동 Popover (Light Dismiss 비활성) -->
<div id="confirm-dialog" popover="manual">
  <p>정말 삭제하시겠습니까?</p>
  <button popovertarget="confirm-dialog" popovertargetaction="hide">취소</button>
  <button onclick="handleDelete()">삭제</button>
</div>
```

```typescript
// utils/popover-compat.ts

function supportsPopover(): boolean {
  return "popover" in HTMLElement.prototype;
}

/**
 * Popover API 미지원 브라우저에서 <dialog>를 Fallback으로 사용한다.
 */
function initPopoverFallback(): void {
  if (supportsPopover()) return;

  document.querySelectorAll<HTMLElement>("[popover]").forEach((el) => {
    el.style.display = "none";
    el.dataset.popoverFallback = "true";
  });

  document.querySelectorAll<HTMLElement>("[popovertarget]").forEach((trigger) => {
    const targetId = trigger.getAttribute("popovertarget");
    if (!targetId) return;

    const target = document.getElementById(targetId);
    if (!target) return;

    const action = trigger.getAttribute("popovertargetaction") ?? "toggle";

    trigger.addEventListener("click", () => {
      const isVisible = target.style.display !== "none";

      if (action === "hide" || (action === "toggle" && isVisible)) {
        target.style.display = "none";
      } else {
        target.style.display = "block";
        target.style.position = "fixed";
        target.style.zIndex = "9999";
        target.style.top = "50%";
        target.style.left = "50%";
        target.style.transform = "translate(-50%, -50%)";
      }
    });
  });
}

export { supportsPopover, initPopoverFallback };
```

### 3.3 CSS `@scope`

컴포넌트 범위를 CSS 레벨에서 한정한다. CSS Modules나 BEM 없이 스타일 격리를 달성한다.

```css
/* @scope: 특정 루트 아래에서만 스타일 적용 */
@scope (.card) {
  :scope {
    padding: 1.5rem;
    border-radius: 12px;
    container-type: inline-size;
  }

  .title {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
  }

  .body {
    color: var(--color-text-secondary, #6b7280);
    line-height: 1.6;
  }
}

/* @scope with lower boundary: 내부 .nested-card까지만 적용 */
@scope (.card) to (.nested-card) {
  p {
    font-size: 0.875rem;
  }
}

/* @scope + Cascade Layers 조합 */
@layer components {
  @scope (.alert) {
    :scope {
      padding: 1rem;
      border-left: 4px solid;
      border-radius: 4px;
    }

    &[data-variant="error"] {
      border-color: var(--color-error, #ef4444);
      background: var(--color-error-bg, #fef2f2);
    }

    &[data-variant="success"] {
      border-color: var(--color-success, #22c55e);
      background: var(--color-success-bg, #f0fdf4);
    }
  }
}

/* Fallback: @scope 미지원 시 직접 선택자 사용 */
@supports not (selector(:scope)) {
  .card .title {
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
  }

  .card .body {
    color: var(--color-text-secondary, #6b7280);
    line-height: 1.6;
  }
}
```

### 3.4 CSS 2026 종합 예제: Anchor + Popover + @scope

```css
@layer components {
  /* 앵커 + 팝오버 + 스코프 조합 */
  @scope (.user-menu) {
    .trigger {
      anchor-name: --user-menu-anchor;
    }

    [popover] {
      position: fixed;
      position-anchor: --user-menu-anchor;
      top: anchor(bottom);
      right: anchor(right);

      margin: 0;
      padding: 0;
      min-width: 200px;
      border-radius: 8px;
      box-shadow: 0 8px 32px rgb(0 0 0 / 0.15);

      position-try-fallbacks: flip-block;
    }

    .menu-item {
      display: block;
      padding: 0.75rem 1rem;
      text-decoration: none;
      color: var(--color-text);

      &:hover {
        background: var(--color-hover-bg, #f3f4f6);
      }
    }
  }
}
```

---

## 4. 멀티 베타 + Preview별 BrowserStack 매트릭스

### 4.1 환경별 테스트 등급 정의

| 환경 | 테스트 범위 | 실행 방식 | 트리거 |
|------|-----------|----------|--------|
| **Production** | Tier 1 + Tier 2 전체 (6종) | Playwright + BrowserStack 실기기 | 릴리스 배포 시 |
| **Beta** | Tier 1 전체 (4종) | Playwright 로컬 + BrowserStack (조건부) | beta 브랜치 push |
| **PR Preview** | Chromium + WebKit | Playwright 로컬 | PR 생성/업데이트 |
| **PR Preview (full)** | Tier 1 + 모바일 2종 | Playwright + BrowserStack | `compat-full` 라벨 |

### 4.2 BrowserStack 매트릭스 상세

| Project Name | 브라우저 | OS / 디바이스 | 버전 | 환경 |
|-------------|---------|-------------|------|------|
| `bs-chrome-win` | Chrome | Windows 11 | latest | Production, Beta, PR(full) |
| `bs-firefox-win` | Firefox | Windows 11 | latest | Production, Beta, PR(full) |
| `bs-safari-mac` | Safari | macOS Sequoia | latest | Production, Beta, PR(full) |
| `bs-edge-win` | Edge | Windows 11 | latest | Production, Beta |
| `bs-chrome-android` | Chrome | Samsung Galaxy S25 / Android 15 | latest | Production, PR(full) |
| `bs-safari-ios` | Safari | iPhone 16 / iOS 18 | latest | Production, PR(full) |
| `bs-samsung-android` | Samsung Internet | Samsung Galaxy S24 / Android 14 | latest | Production |

### 4.3 매트릭스 실행 스크립트

```typescript
// scripts/run-compat-matrix.ts
import { execSync } from "node:child_process";

type EnvironmentTier = "production" | "beta" | "preview" | "preview-full";

interface MatrixConfig {
  environments: string[];
  tier: EnvironmentTier;
  testPattern: string;
}

interface MatrixResult {
  environment: string;
  browser: string;
  passed: number;
  failed: number;
  skipped: number;
  duration: number;
}

const BROWSER_MATRIX: Record<EnvironmentTier, string[]> = {
  production: [
    "chromium", "firefox", "webkit",
    "bs-chrome-win", "bs-firefox-win", "bs-safari-mac", "bs-edge-win",
    "bs-chrome-android", "bs-safari-ios", "bs-samsung-android",
  ],
  beta: ["chromium", "firefox", "webkit", "bs-chrome-win", "bs-firefox-win", "bs-safari-mac", "bs-edge-win"],
  preview: ["chromium", "webkit"],
  "preview-full": [
    "chromium", "firefox", "webkit",
    "bs-chrome-win", "bs-safari-mac", "bs-chrome-android", "bs-safari-ios",
  ],
};

function runTestMatrix(config: MatrixConfig): MatrixResult[] {
  const browsers = BROWSER_MATRIX[config.tier];
  const results: MatrixResult[] = [];

  for (const env of config.environments) {
    for (const browser of browsers) {
      const startTime = Date.now();
      const isBrowserStack = browser.startsWith("bs-");

      try {
        const envVars: Record<string, string> = {
          ...process.env as Record<string, string>,
          TEST_ENVIRONMENTS: env,
          PLAYWRIGHT_JSON_OUTPUT_FILE: `test-results/${env}-${browser}.json`,
        };

        if (isBrowserStack) {
          envVars.USE_BROWSERSTACK = "true";
        }

        const output = execSync(
          `npx playwright test --project=${browser} --grep="${config.testPattern}" --reporter=json`,
          { encoding: "utf-8", env: envVars, maxBuffer: 50 * 1024 * 1024 },
        );

        const report = JSON.parse(output);
        results.push({
          environment: env,
          browser,
          passed: report.stats?.expected ?? 0,
          failed: report.stats?.unexpected ?? 0,
          skipped: report.stats?.skipped ?? 0,
          duration: Date.now() - startTime,
        });
      } catch {
        results.push({
          environment: env,
          browser,
          passed: 0,
          failed: 1,
          skipped: 0,
          duration: Date.now() - startTime,
        });
      }
    }
  }

  return results;
}

function generateMarkdownReport(results: MatrixResult[]): string {
  const header = "| 환경 | 브라우저 | 성공 | 실패 | 스킵 | 시간 |";
  const separator = "|------|---------|------|------|------|------|";
  const rows = results.map(
    (r) =>
      `| ${r.environment} | ${r.browser} | ${r.passed} | ${r.failed} | ${r.skipped} | ${(r.duration / 1000).toFixed(1)}s |`,
  );

  const totalPassed = results.reduce((sum, r) => sum + r.passed, 0);
  const totalFailed = results.reduce((sum, r) => sum + r.failed, 0);
  const status = totalFailed === 0 ? "ALL PASSED" : `${totalFailed} FAILURES`;

  return [`## 크로스 브라우저 테스트 매트릭스 결과 (${status})`, "", header, separator, ...rows].join("\n");
}

// 실행
const tier = (process.env.MATRIX_TIER as EnvironmentTier) ?? "preview";
const config: MatrixConfig = {
  environments: (process.env.TEST_ENVIRONMENTS ?? "production").split(","),
  tier,
  testPattern: "compatibility|visual",
};

const results = runTestMatrix(config);
console.log(generateMarkdownReport(results));

export { runTestMatrix, generateMarkdownReport, BROWSER_MATRIX };
export type { MatrixConfig, MatrixResult, EnvironmentTier };
```

---

## 5. Import Maps

번들러 없이 브라우저에서 직접 bare module specifier를 해석할 수 있다. 개발 환경, 마이크로 프론트엔드, CDN 기반 배포에서 유용하다.

### 5.1 기본 사용

```html
<script type="importmap">
{
  "imports": {
    "react": "https://esm.sh/react@19.1.0",
    "react-dom/client": "https://esm.sh/react-dom@19.1.0/client",
    "lodash-es/": "https://esm.sh/lodash-es@4.17.21/",
    "@company/design-system": "/packages/design-system/index.js",
    "#utils/": "/src/utils/"
  },
  "scopes": {
    "/legacy/": {
      "react": "https://esm.sh/react@18.3.1"
    }
  }
}
</script>

<script type="module">
  // Import Map이 해석하여 CDN URL로 변환
  import React from "react";
  import { createRoot } from "react-dom/client";
  import { debounce } from "lodash-es/debounce";
  import { Button } from "@company/design-system";
</script>
```

### 5.2 환경별 Import Map 동적 생성

```typescript
// scripts/generate-importmap.ts

interface ImportMapConfig {
  imports: Record<string, string>;
  scopes?: Record<string, Record<string, string>>;
}

type DeployEnvironment = "production" | "staging" | "development";

function generateImportMap(env: DeployEnvironment): ImportMapConfig {
  const cdnBase: Record<DeployEnvironment, string> = {
    production: "https://cdn.example.com/modules",
    staging: "https://staging-cdn.example.com/modules",
    development: "http://localhost:3000/node_modules",
  };

  const base = cdnBase[env];
  const version = process.env.DEPLOY_VERSION ?? "latest";

  return {
    imports: {
      "react": `${base}/react@19/${version}/react.production.min.js`,
      "react-dom/client": `${base}/react-dom@19/${version}/client.production.min.js`,
      "@company/shared": `${base}/@company/shared@${version}/index.js`,
      "#components/": `${base}/@company/components@${version}/`,
    },
    scopes: env === "development"
      ? { "/test/": { "react": `${base}/react@19/development/react.development.js` } }
      : undefined,
  };
}

/** HTML에 삽입할 <script type="importmap"> 태그를 생성한다 */
function renderImportMapTag(config: ImportMapConfig): string {
  return `<script type="importmap">\n${JSON.stringify(config, null, 2)}\n</script>`;
}

export { generateImportMap, renderImportMapTag };
export type { ImportMapConfig, DeployEnvironment };
```

### 5.3 Import Maps Feature Detection

```typescript
// utils/importmap-detect.ts

function supportsImportMaps(): boolean {
  return HTMLScriptElement.supports?.("importmap") ?? false;
}

/**
 * Import Maps 미지원 브라우저에서 es-module-shims를 로드한다.
 * es-module-shims는 Import Maps를 폴리필하는 경량 라이브러리이다.
 */
function ensureImportMapsSupport(): void {
  if (supportsImportMaps()) return;

  const shimScript = document.createElement("script");
  shimScript.async = true;
  shimScript.src = "https://ga.jspm.io/npm:es-module-shims@2.0.0/dist/es-module-shims.js";
  document.head.prepend(shimScript);
}

export { supportsImportMaps, ensureImportMapsSupport };
```

---

## 6. UA Client Hints

`navigator.userAgent` 문자열 파싱을 대체하는 구조화된 API이다. Privacy-preserving이며 서버/클라이언트 양쪽에서 사용할 수 있다.

### 6.1 클라이언트 측 사용

```typescript
// utils/client-hints.ts

interface ParsedClientHints {
  brands: Array<{ brand: string; version: string }>;
  mobile: boolean;
  platform: string;
  platformVersion: string;
  architecture: string;
  model: string;
  fullVersionList: Array<{ brand: string; version: string }>;
}

/**
 * UA Client Hints API를 사용하여 브라우저 정보를 가져온다.
 * 미지원 브라우저에서는 navigator.userAgent를 파싱하여 Fallback한다.
 */
async function getClientHints(): Promise<ParsedClientHints> {
  // Client Hints High-Entropy API 지원 확인
  if ("userAgentData" in navigator && navigator.userAgentData) {
    const uaData = navigator.userAgentData as NavigatorUAData;

    // Low-entropy: 항상 사용 가능
    const lowEntropy: Partial<ParsedClientHints> = {
      brands: uaData.brands.map((b) => ({ brand: b.brand, version: b.version })),
      mobile: uaData.mobile,
      platform: uaData.platform,
    };

    // High-entropy: 사용자 동의 필요 (Permissions-Policy)
    try {
      const highEntropy = await uaData.getHighEntropyValues([
        "platformVersion",
        "architecture",
        "model",
        "fullVersionList",
      ]);

      return {
        ...lowEntropy,
        platformVersion: highEntropy.platformVersion ?? "",
        architecture: highEntropy.architecture ?? "",
        model: highEntropy.model ?? "",
        fullVersionList: highEntropy.fullVersionList ?? [],
      } as ParsedClientHints;
    } catch {
      return {
        ...lowEntropy,
        platformVersion: "",
        architecture: "",
        model: "",
        fullVersionList: [],
      } as ParsedClientHints;
    }
  }

  // Fallback: UA 문자열 파싱 (Safari, Firefox 등)
  return parseUserAgentFallback(navigator.userAgent);
}

function parseUserAgentFallback(ua: string): ParsedClientHints {
  const mobile = /Mobi|Android/i.test(ua);
  const platform = /Mac/i.test(ua) ? "macOS"
    : /Win/i.test(ua) ? "Windows"
    : /Linux/i.test(ua) ? "Linux"
    : /Android/i.test(ua) ? "Android"
    : /iPhone|iPad/i.test(ua) ? "iOS"
    : "Unknown";

  const brands: Array<{ brand: string; version: string }> = [];
  const chromeMatch = ua.match(/Chrome\/([\d.]+)/);
  const firefoxMatch = ua.match(/Firefox\/([\d.]+)/);
  const safariMatch = ua.match(/Version\/([\d.]+).*Safari/);

  if (chromeMatch) brands.push({ brand: "Chromium", version: chromeMatch[1] });
  if (firefoxMatch) brands.push({ brand: "Firefox", version: firefoxMatch[1] });
  if (safariMatch) brands.push({ brand: "Safari", version: safariMatch[1] });

  return {
    brands,
    mobile,
    platform,
    platformVersion: "",
    architecture: "",
    model: "",
    fullVersionList: brands,
  };
}

/** UA Client Hints를 사용하여 Tier 1 브라우저인지 판별한다 */
async function isTier1Browser(): Promise<boolean> {
  const hints = await getClientHints();
  const tier1Brands = ["Chromium", "Google Chrome", "Firefox", "Safari", "Microsoft Edge"];
  return hints.brands.some((b) => tier1Brands.includes(b.brand));
}

// TypeScript 타입 보강
interface NavigatorUABrandVersion {
  brand: string;
  version: string;
}

interface NavigatorUAData {
  brands: NavigatorUABrandVersion[];
  mobile: boolean;
  platform: string;
  getHighEntropyValues(hints: string[]): Promise<Record<string, unknown>>;
}

export { getClientHints, isTier1Browser, parseUserAgentFallback };
export type { ParsedClientHints };
```

### 6.2 서버 측: HTTP Client Hints 요청

```typescript
// middleware/client-hints.ts
// Express/Hono 등 서버 프레임워크에서 사용

interface ClientHintsMiddlewareConfig {
  /** Critical-CH로 요청할 힌트 (첫 요청에서 재전송) */
  critical: string[];
  /** Accept-CH로 요청할 힌트 */
  accept: string[];
}

const DEFAULT_CONFIG: ClientHintsMiddlewareConfig = {
  critical: ["Sec-CH-UA", "Sec-CH-UA-Mobile", "Sec-CH-UA-Platform"],
  accept: [
    "Sec-CH-UA",
    "Sec-CH-UA-Mobile",
    "Sec-CH-UA-Platform",
    "Sec-CH-UA-Platform-Version",
    "Sec-CH-UA-Full-Version-List",
    "Sec-CH-UA-Arch",
    "Sec-CH-UA-Model",
  ],
};

/**
 * Client Hints 요청 헤더를 응답에 설정하는 헬퍼이다.
 * Permissions-Policy와 함께 사용한다.
 */
function buildClientHintsHeaders(
  config: ClientHintsMiddlewareConfig = DEFAULT_CONFIG,
): Record<string, string> {
  return {
    "Accept-CH": config.accept.join(", "),
    "Critical-CH": config.critical.join(", "),
    "Permissions-Policy": config.accept
      .map((h) => `${h.toLowerCase().replace("sec-ch-", "ch-")}=(self)`)
      .join(", "),
  };
}

/**
 * 요청 헤더에서 Client Hints를 파싱한다.
 */
function parseClientHintsFromHeaders(headers: Record<string, string | undefined>): {
  brands: string;
  mobile: boolean;
  platform: string;
} {
  return {
    brands: headers["sec-ch-ua"] ?? "",
    mobile: headers["sec-ch-ua-mobile"] === "?1",
    platform: headers["sec-ch-ua-platform"]?.replace(/"/g, "") ?? "",
  };
}

export { buildClientHintsHeaders, parseClientHintsFromHeaders };
export type { ClientHintsMiddlewareConfig };
```

---

## 7. 환경별 Polyfill 전략

### 7.1 전략 개요

| 전략 | 적용 환경 | 방식 | 장점 | 단점 |
|------|----------|------|------|------|
| **조건부 동적 import** | SPA, CSR | Feature Detection + `import()` | 필요한 Polyfill만 로드, 번들 분리 | 초기 로드 시 딜레이 |
| **Differential Serving** | SSR, MPA | `<script type="module">` + `<script nomodule>` | 모던 브라우저는 Polyfill 0개 | 빌드 파이프라인 복잡 |
| **CDN Polyfill Service** | 빠른 프로토타이핑 | polyfill.io 대체 서비스 (cdnjs, polyfill-fastly) | 설정 간단 | 외부 의존성, 서비스 중단 리스크 |
| **core-js 선택적 임포트** | 모든 환경 | 필요한 모듈만 `import` | 정밀 제어, 트리셰이킹 | 수동 관리 필요 |

### 7.2 조건부 동적 import 패턴

```typescript
// polyfills/index.ts

interface PolyfillEntry {
  name: string;
  test: () => boolean;
  load: () => Promise<unknown>;
  /** gzip 기준 예상 크기 (KB) */
  estimatedSize: number;
}

const POLYFILL_REGISTRY: PolyfillEntry[] = [
  {
    name: "Object.groupBy",
    test: () => typeof Object.groupBy === "function",
    load: () => import("core-js/actual/object/group-by"),
    estimatedSize: 0.5,
  },
  {
    name: "Promise.withResolvers",
    test: () => typeof Promise.withResolvers === "function",
    load: () => import("core-js/actual/promise/with-resolvers"),
    estimatedSize: 0.3,
  },
  {
    name: "Set methods",
    test: () => typeof Set.prototype.union === "function",
    load: () => import("core-js/actual/set"),
    estimatedSize: 2.1,
  },
  {
    name: "Map.groupBy",
    test: () => typeof Map.groupBy === "function",
    load: () => import("core-js/actual/map/group-by"),
    estimatedSize: 0.5,
  },
  {
    name: "Iterator helpers",
    test: () => typeof Iterator.prototype?.map === "function",
    load: () => import("core-js/actual/iterator"),
    estimatedSize: 3.2,
  },
  {
    name: "Array.fromAsync",
    test: () => typeof Array.fromAsync === "function",
    load: () => import("core-js/actual/array/from-async"),
    estimatedSize: 0.4,
  },
];

async function loadPolyfills(): Promise<{ loaded: string[]; skipped: string[] }> {
  const needed = POLYFILL_REGISTRY.filter((p) => {
    try {
      return !p.test();
    } catch {
      return true;
    }
  });

  if (needed.length === 0) {
    return { loaded: [], skipped: POLYFILL_REGISTRY.map((p) => p.name) };
  }

  const totalSize = needed.reduce((sum, p) => sum + p.estimatedSize, 0);
  console.info(
    `[Polyfill] ${needed.length}개 로드 시작 (예상 ${totalSize.toFixed(1)}KB gzip)`,
  );

  await Promise.all(needed.map((p) => p.load()));

  const loadedNames = needed.map((p) => p.name);
  console.info(`[Polyfill] 로드 완료: ${loadedNames.join(", ")}`);

  return {
    loaded: loadedNames,
    skipped: POLYFILL_REGISTRY.filter((p) => !needed.includes(p)).map((p) => p.name),
  };
}

export { loadPolyfills, POLYFILL_REGISTRY };
export type { PolyfillEntry };
```

### 7.3 Differential Serving (module / nomodule)

```html
<!-- 모던 브라우저: ES2025 빌드 (Polyfill 없음) -->
<script type="module" src="/assets/app.modern.js"></script>

<!-- 레거시 브라우저: ES5 빌드 + 필수 Polyfill -->
<script nomodule src="/assets/polyfills.legacy.js"></script>
<script nomodule src="/assets/app.legacy.js"></script>
```

```typescript
// vite.config.ts -- Differential Build 설정 예시
import { defineConfig } from "vite";
import legacy from "@vitejs/plugin-legacy";

export default defineConfig({
  plugins: [
    legacy({
      targets: ["defaults", "not IE 11"],
      modernPolyfills: [
        "es.object.group-by",
        "es.promise.with-resolvers",
      ],
      renderLegacyChunks: true,
    }),
  ],
});
```

### 7.4 CSS Polyfill (Feature Detection 기반)

```typescript
// polyfills/css-polyfills.ts

async function loadCssPolyfills(): Promise<void> {
  const polyfills: Promise<unknown>[] = [];

  // Container Queries Polyfill
  if (!CSS.supports("container-type", "inline-size")) {
    polyfills.push(
      import("container-query-polyfill").then((mod) => mod.default?.()),
    );
  }

  // :has() Polyfill (CSS 전용 Polyfill은 없으므로 JS 기반 대체)
  if (!CSS.supports("selector(:has(*))")) {
    console.warn("[CSS Polyfill] :has() 미지원. Fallback 클래스 전략을 사용하세요.");
  }

  if (polyfills.length > 0) {
    await Promise.all(polyfills);
  }
}

export { loadCssPolyfills };
```

---

## 8. Playwright 크로스 브라우저 자동화

### 8.1 Playwright + BrowserStack 통합 설정

```typescript
// playwright.config.ts
import { defineConfig, devices } from "@playwright/test";

type EnvironmentType = "production" | "beta" | "preview";

interface TestEnvironment {
  name: string;
  type: EnvironmentType;
  baseURL: string;
}

function resolveEnvironments(): TestEnvironment[] {
  const cdnDomain = process.env.CDN_DOMAIN ?? "app.example.com";
  const envList = process.env.TEST_ENVIRONMENTS ?? "production";

  return envList.split(",").map((env) => {
    const trimmed = env.trim();
    if (trimmed === "production") {
      return { name: "production", type: "production", baseURL: `https://${cdnDomain}` };
    }
    if (trimmed.startsWith("beta-")) {
      return { name: trimmed, type: "beta", baseURL: `https://${cdnDomain}/${trimmed}` };
    }
    return { name: trimmed, type: "preview", baseURL: `https://${cdnDomain}/${trimmed}` };
  });
}

function buildBrowserStackWsUrl(
  browser: string,
  os: string,
  browserVersion: string,
  device?: string,
): string {
  const caps = {
    browser,
    os,
    browser_version: browserVersion,
    ...(device ? { device } : {}),
    "browserstack.username": process.env.BROWSERSTACK_USERNAME,
    "browserstack.accessKey": process.env.BROWSERSTACK_ACCESS_KEY,
    "browserstack.playwrightVersion": "1.50.0",
    project: "Multi-Beta Compat Test",
    build: `${process.env.TEST_ENVIRONMENT ?? "local"}-${Date.now()}`,
  };
  return `wss://cdp.browserstack.com/playwright?caps=${encodeURIComponent(JSON.stringify(caps))}`;
}

function buildBrowserStackProjects() {
  return [
    {
      name: "bs-chrome-win",
      use: { connectOptions: { wsEndpoint: buildBrowserStackWsUrl("chrome", "Windows 11", "latest") } },
    },
    {
      name: "bs-firefox-win",
      use: { connectOptions: { wsEndpoint: buildBrowserStackWsUrl("firefox", "Windows 11", "latest") } },
    },
    {
      name: "bs-safari-mac",
      use: { connectOptions: { wsEndpoint: buildBrowserStackWsUrl("safari", "OS X Sequoia", "latest") } },
    },
    {
      name: "bs-edge-win",
      use: { connectOptions: { wsEndpoint: buildBrowserStackWsUrl("edge", "Windows 11", "latest") } },
    },
    {
      name: "bs-chrome-android",
      use: {
        connectOptions: {
          wsEndpoint: buildBrowserStackWsUrl("chrome", "Android 15", "latest", "Samsung Galaxy S25"),
        },
      },
    },
    {
      name: "bs-safari-ios",
      use: {
        connectOptions: {
          wsEndpoint: buildBrowserStackWsUrl("safari", "iOS 18", "latest", "iPhone 16"),
        },
      },
    },
    {
      name: "bs-samsung-android",
      use: {
        connectOptions: {
          wsEndpoint: buildBrowserStackWsUrl("samsung", "Android 14", "latest", "Samsung Galaxy S24"),
        },
      },
    },
  ];
}

const environments = resolveEnvironments();
const useBrowserStack = process.env.USE_BROWSERSTACK === "true";

export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 4 : undefined,

  reporter: [
    ["html", { open: "never" }],
    ["json", { outputFile: "test-results/report.json" }],
    ...(process.env.CI ? [["github" as const]] : []),
  ],

  use: {
    baseURL: environments[0]?.baseURL ?? "http://localhost:3000",
    trace: "on-first-retry",
    screenshot: "only-on-failure",
    video: "on-first-retry",
  },

  projects: [
    { name: "chromium", use: { ...devices["Desktop Chrome"] } },
    { name: "firefox", use: { ...devices["Desktop Firefox"] } },
    { name: "webkit", use: { ...devices["Desktop Safari"] } },
    { name: "mobile-chrome", use: { ...devices["Pixel 8"] } },
    { name: "mobile-safari", use: { ...devices["iPhone 16"] } },
    ...(useBrowserStack ? buildBrowserStackProjects() : []),
  ],

  webServer: process.env.CI
    ? undefined
    : { command: "pnpm dev", port: 3000, reuseExistingServer: true },
});
```

### 8.2 크로스 브라우저 호환성 테스트 스펙

```typescript
// e2e/cross-browser-compat.spec.ts
import { test, expect } from "@playwright/test";

const TARGET_ENV = process.env.TEST_ENVIRONMENTS ?? "production";

test.describe(`[${TARGET_ENV}] 크로스 브라우저 호환성`, () => {
  // --- CSS 2026 기능 테스트 ---

  test("CSS Container Queries 동작 확인", async ({ page }) => {
    await page.goto("/components/card-grid");
    await page.waitForLoadState("networkidle");

    await page.setViewportSize({ width: 1200, height: 800 });
    const cardBody = page.locator(".card-body").first();
    await expect(cardBody).toHaveCSS("display", "grid");

    await page.setViewportSize({ width: 400, height: 800 });
    await expect(cardBody).toHaveCSS("display", "flex");
  });

  test("CSS :has() 선택자 동작 확인", async ({ page }) => {
    await page.goto("/components/form");

    const emailInput = page.locator('input[type="email"]');
    await emailInput.fill("invalid-email");
    await emailInput.blur();

    const formGroup = page.locator(".form-group").filter({ has: emailInput });
    await expect(formGroup).toHaveCSS("border-color", "rgb(239, 68, 68)");
  });

  test("CSS Nesting 동작 확인", async ({ page }) => {
    await page.goto("/components/nav");

    const link = page.locator(".nav a").first();
    const defaultColor = await link.evaluate((el) => getComputedStyle(el).color);
    expect(defaultColor).toBeTruthy();

    await link.hover();
    const hoverColor = await link.evaluate((el) => getComputedStyle(el).color);
    expect(hoverColor).not.toBe(defaultColor);
  });

  test("Popover API 동작 확인", async ({ page }) => {
    await page.goto("/components/popover-demo");

    const hasPopover = await page.evaluate(() => "popover" in HTMLElement.prototype);

    if (hasPopover) {
      // 네이티브 Popover 테스트
      const trigger = page.locator("[popovertarget='test-popover']");
      await trigger.click();

      const popover = page.locator("#test-popover");
      await expect(popover).toBeVisible();

      // Light Dismiss (배경 클릭)
      await page.mouse.click(10, 10);
      await expect(popover).not.toBeVisible();
    } else {
      // Fallback 동작 테스트
      const trigger = page.locator("[data-popover-trigger]");
      await trigger.click();

      const popover = page.locator("[data-popover-fallback]");
      await expect(popover).toBeVisible();
    }
  });

  test("CSS Anchor Positioning Fallback 확인", async ({ page }) => {
    await page.goto("/components/dropdown");

    const hasAnchor = await page.evaluate(() => CSS.supports("position-anchor", "--test"));

    const trigger = page.locator(".dropdown-trigger").first();
    await trigger.click();

    const menu = page.locator(".dropdown-menu").first();
    await expect(menu).toBeVisible();

    // 메뉴가 트리거 아래에 위치하는지 확인 (네이티브/Fallback 모두 동일)
    const triggerBox = await trigger.boundingBox();
    const menuBox = await menu.boundingBox();

    if (triggerBox && menuBox) {
      expect(menuBox.y).toBeGreaterThanOrEqual(triggerBox.y + triggerBox.height - 2);
    }

    // 로그에 사용된 방식 기록
    test.info().annotations.push({
      type: "anchor-positioning",
      description: hasAnchor ? "native" : "js-fallback",
    });
  });

  // --- View Transitions ---

  test("View Transitions API 동작 확인", async ({ page }) => {
    await page.goto("/");

    const hasViewTransitions = await page.evaluate(() => "startViewTransition" in document);

    await page.click('a[href$="/about"]');

    if (hasViewTransitions) {
      await page.waitForFunction(
        () => !document.querySelector("::view-transition"),
        null,
        { timeout: 3000 },
      ).catch(() => { /* 전환 완료 또는 미지원 */ });
    }

    await expect(page).toHaveURL(/\/about$/);
  });

  // --- Import Maps ---

  test("Import Maps 또는 Shim 정상 로드 확인", async ({ page }) => {
    await page.goto("/");

    const moduleStatus = await page.evaluate(() => {
      const importMapScript = document.querySelector('script[type="importmap"]');
      const shimScript = document.querySelector('script[src*="es-module-shims"]');
      return {
        hasImportMap: !!importMapScript,
        hasShim: !!shimScript,
        supportsImportMap: HTMLScriptElement.supports?.("importmap") ?? false,
      };
    });

    // Import Map이 있거나 Shim이 있어야 한다
    expect(moduleStatus.hasImportMap || moduleStatus.hasShim).toBe(true);
  });

  // --- 시각적 스냅샷 ---

  test("시각적 스냅샷 비교", async ({ page, browserName }) => {
    const routes = [
      { name: "home", path: "/" },
      { name: "about", path: "/about" },
    ];

    for (const { name, path } of routes) {
      await page.goto(path);
      await page.waitForLoadState("networkidle");

      await expect(page).toHaveScreenshot(
        `${TARGET_ENV}-${browserName}-${name}.png`,
        { maxDiffPixelRatio: 0.02, fullPage: true },
      );
    }
  });
});
```

---

## 9. AI 기반 호환성 이슈 자동 탐지

### 9.1 eslint-plugin-compat + AI 분석 파이프라인

```typescript
// scripts/ai-compat-check.ts
import { execSync } from "node:child_process";
import { writeFileSync } from "node:fs";

interface CompatIssue {
  file: string;
  line: number;
  rule: string;
  message: string;
  api: string;
}

function runEslintCompat(): CompatIssue[] {
  const result = execSync(
    'npx eslint --format json --rule \'{"compat/compat": "error"}\' src/',
    { encoding: "utf-8", maxBuffer: 10 * 1024 * 1024 },
  );

  const parsed = JSON.parse(result) as Array<{
    filePath: string;
    messages: Array<{ line: number; ruleId: string; message: string }>;
  }>;

  return parsed.flatMap((file) =>
    file.messages
      .filter((msg) => msg.ruleId === "compat/compat")
      .map((msg) => ({
        file: file.filePath,
        line: msg.line,
        rule: msg.ruleId,
        message: msg.message,
        api: msg.message.match(/'([^']+)'/)?.[1] ?? "unknown",
      })),
  );
}

function generateAiPrompt(issues: CompatIssue[]): string {
  const grouped = Object.groupBy(issues, (i) => i.api);
  const summary = Object.entries(grouped)
    .map(
      ([api, items]) =>
        `- ${api}: ${items!.length}건 (${items!.map((i) => `${i.file}:${i.line}`).join(", ")})`,
    )
    .join("\n");

  return [
    "다음 호환성 이슈에 대해 각각:",
    "(1) Polyfill이 있으면 npm 패키지명과 설치 코드",
    "(2) Polyfill이 없으면 대체 구현 (TypeScript)",
    "(3) Feature Detection 코드",
    "(4) 사용자 영향 범위 추정",
    "",
    `--- 이슈 목록 (${issues.length}건) ---`,
    summary,
  ].join("\n");
}

const issues = runEslintCompat();
const prompt = generateAiPrompt(issues);
writeFileSync("compat-ai-prompt.txt", prompt, "utf-8");
console.log(`[AI Compat] ${issues.length}건 발견. compat-ai-prompt.txt 저장 완료.`);

export { runEslintCompat, generateAiPrompt };
export type { CompatIssue };
```

### 9.2 테스트 결과 AI 분석 스크립트

```typescript
// scripts/analyze-test-results.ts
import { readFileSync } from "node:fs";

interface TestResult {
  title: string;
  status: "passed" | "failed" | "skipped";
  browser: string;
  duration: number;
  error?: string;
  environment?: string;
}

function parsePlaywrightReport(reportPath: string): TestResult[] {
  const raw = readFileSync(reportPath, "utf-8");
  const report = JSON.parse(raw) as {
    suites: Array<{
      specs: Array<{
        title: string;
        tests: Array<{
          projectName: string;
          status: string;
          duration: number;
          results: Array<{ error?: { message: string } }>;
        }>;
      }>;
    }>;
  };

  return report.suites.flatMap((suite) =>
    suite.specs.flatMap((spec) =>
      spec.tests.map((t) => ({
        title: spec.title,
        status: t.status as TestResult["status"],
        browser: t.projectName,
        duration: t.duration,
        error: t.results[0]?.error?.message,
      })),
    ),
  );
}

function buildAnalysisPrompt(results: TestResult[]): string {
  const failures = results.filter((r) => r.status === "failed");
  const byBrowser = Object.groupBy(failures, (f) => f.browser);

  return [
    "다음은 Playwright 크로스 브라우저 테스트 실패 결과이다.",
    "브라우저별 실패 원인을 분석하고, 호환성 문제 / 로직 버그 / 환경 차이로 분류해줘.",
    "각 문제에 대해 수정 코드를 TypeScript로 제시하고 우선순위(P0~P3)를 매겨줘.",
    "",
    `--- 실패 목록 (${failures.length}건) ---`,
    failures.map((f) => `[${f.browser}] ${f.title}: ${f.error ?? "unknown"}`).join("\n"),
    "",
    "--- 브라우저별 실패 수 ---",
    Object.entries(byBrowser).map(([b, fs]) => `${b}: ${fs!.length}건`).join("\n"),
  ].join("\n");
}

const results = parsePlaywrightReport("test-results/report.json");
console.log(buildAnalysisPrompt(results));

export { parsePlaywrightReport, buildAnalysisPrompt };
export type { TestResult };
```

---

## 10. Preview 환경 호환성 자동 검증 CI

```yaml
# .github/workflows/preview-compat-test.yml
name: Preview Compatibility Test

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write
  checks: write

concurrency:
  group: compat-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    outputs:
      preview_url: ${{ steps.deploy.outputs.preview_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build
        env:
          VITE_BASE_PATH: /pr-${{ github.event.pull_request.number }}/

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ap-northeast-2

      - name: Deploy to S3
        id: deploy
        run: |
          aws s3 sync dist/ \
            s3://${{ vars.PREVIEW_BUCKET }}/pr-${{ github.event.pull_request.number }}/ \
            --delete
          echo "preview_url=https://${{ vars.CDN_DOMAIN }}/pr-${{ github.event.pull_request.number }}/" >> "$GITHUB_OUTPUT"

  cross-browser-test:
    needs: deploy-preview
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]
        include:
          - browser: chromium
            label: Chrome
          - browser: firefox
            label: Firefox
          - browser: webkit
            label: Safari

    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run compatibility tests
        run: |
          npx playwright test \
            --project=${{ matrix.browser }} \
            e2e/cross-browser-compat.spec.ts
        env:
          TEST_ENVIRONMENTS: pr-${{ github.event.pull_request.number }}
          PLAYWRIGHT_BASE_URL: ${{ needs.deploy-preview.outputs.preview_url }}

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-${{ matrix.browser }}
          path: test-results/
          retention-days: 7

  browserstack-test:
    needs: deploy-preview
    runs-on: ubuntu-latest
    if: contains(github.event.pull_request.labels.*.name, 'compat-full')
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Run BrowserStack tests
        run: |
          npx playwright test \
            --project=bs-chrome-win \
            --project=bs-firefox-win \
            --project=bs-safari-mac \
            --project=bs-chrome-android \
            --project=bs-safari-ios \
            e2e/cross-browser-compat.spec.ts
        env:
          TEST_ENVIRONMENTS: pr-${{ github.event.pull_request.number }}
          PLAYWRIGHT_BASE_URL: ${{ needs.deploy-preview.outputs.preview_url }}
          USE_BROWSERSTACK: "true"
          BROWSERSTACK_USERNAME: ${{ secrets.BROWSERSTACK_USERNAME }}
          BROWSERSTACK_ACCESS_KEY: ${{ secrets.BROWSERSTACK_ACCESS_KEY }}

  report:
    needs: [cross-browser-test, browserstack-test]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: all-results/

      - name: Generate compatibility report
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const prNumber = context.payload.pull_request.number;

            let reportBody = '## 크로스 브라우저 호환성 테스트 결과\n\n';
            reportBody += '| 브라우저 | 결과 | 세부사항 |\n';
            reportBody += '|---------|------|----------|\n';

            const resultDirs = fs.readdirSync('all-results/')
              .filter(d => d.startsWith('test-results-'));

            for (const dir of resultDirs) {
              const browser = dir.replace('test-results-', '');
              const reportPath = path.join('all-results', dir, 'report.json');
              if (fs.existsSync(reportPath)) {
                const report = JSON.parse(fs.readFileSync(reportPath, 'utf-8'));
                const passed = report.stats?.expected ?? 0;
                const failed = report.stats?.unexpected ?? 0;
                const status = failed === 0 ? 'PASS' : 'FAIL';
                reportBody += `| ${browser} | ${status} | ${passed} passed, ${failed} failed |\n`;
              } else {
                reportBody += `| ${browser} | SKIP | 결과 파일 없음 |\n`;
              }
            }

            reportBody += '\n> `compat-full` 라벨을 추가하면 BrowserStack 실기기 테스트도 실행됩니다.';

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
            });
            const existing = comments.find(c =>
              c.body?.includes('크로스 브라우저 호환성 테스트')
            );
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body: reportBody,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: reportBody,
              });
            }
```

---

## 11. 체크리스트

### 설정

- [ ] `.browserslistrc` 설정 및 Tier 1/2/3 정의
- [ ] `eslint-plugin-compat` 설치 및 CI 연동
- [ ] Playwright + BrowserStack 설정 완료
- [ ] Baseline 2026 기준 API 사용 여부 확인

### CSS 2026

- [ ] Cascade Layers (`@layer`)로 스타일 우선순위 관리
- [ ] Container Queries 적용 시 `@supports` Fallback 포함
- [ ] CSS Nesting 사용 시 `&` 구문 준수
- [ ] Anchor Positioning 사용 시 JS Fallback 구현
- [ ] Popover API 사용 시 `<dialog>` 기반 Fallback 구현
- [ ] `@scope` 사용 시 직접 선택자 Fallback 포함

### 멀티 베타 환경

- [ ] PR Preview 자동 배포 워크플로우 구성
- [ ] 환경별 차등 테스트 매트릭스 정의 (Production > Beta > Preview)
- [ ] BrowserStack 실기기 테스트 `compat-full` 라벨 트리거 설정
- [ ] PR 코멘트에 호환성 테스트 결과 자동 게시

### Import Maps / UA Client Hints

- [ ] Import Maps 사용 시 `es-module-shims` Fallback 설정
- [ ] 환경별 Import Map 동적 생성 스크립트 구성
- [ ] UA Client Hints 헤더 설정 (`Accept-CH`, `Critical-CH`)
- [ ] UA 파싱 코드를 Client Hints API로 마이그레이션

### Polyfill

- [ ] Feature Detection 유틸 작성 (`baseline-detect.ts`)
- [ ] 조건부 동적 import Polyfill 로더 구현
- [ ] Differential Serving (`module` / `nomodule`) 설정
- [ ] Polyfill 번들 크기 모니터링 (예상 크기 주석 관리)

### AI 활용

- [ ] AI 프롬프트 5선 팀 공유 및 프로젝트 Wiki 등록
- [ ] eslint-plugin-compat 결과 AI 분석 파이프라인 적용
- [ ] 테스트 실패 결과 AI 분석 자동화 스크립트 적용
- [ ] 브라우저 지원 범위 분기별 재검토 (AI 프롬프트 5번 활용)

---

## 12. 참고 자료

- [Web Platform Baseline](https://web.dev/baseline)
- [Can I Use](https://caniuse.com)
- [Web Platform Status](https://webstatus.dev)
- [eslint-plugin-compat](https://github.com/amilajack/eslint-plugin-compat)
- [Playwright Cross-Browser Testing](https://playwright.dev/docs/browsers)
- [BrowserStack Playwright Integration](https://www.browserstack.com/docs/automate/playwright)
- [CSS Anchor Positioning (MDN)](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_anchor_positioning)
- [Popover API (MDN)](https://developer.mozilla.org/en-US/docs/Web/API/Popover_API)
- [CSS @scope (MDN)](https://developer.mozilla.org/en-US/docs/Web/CSS/@scope)
- [Import Maps (MDN)](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/script/type/importmap)
- [UA Client Hints (MDN)](https://developer.mozilla.org/en-US/docs/Web/API/User-Agent_Client_Hints_API)
- [Container Queries (MDN)](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_containment/Container_queries)
- [View Transitions API (MDN)](https://developer.mozilla.org/en-US/docs/Web/API/View_Transitions_API)
- [CSS Cascade Layers (MDN)](https://developer.mozilla.org/en-US/docs/Learn/CSS/Building_blocks/Cascade_layers)
- [es-module-shims](https://github.com/guybedford/es-module-shims)
- [core-js](https://github.com/nicolo-ribaudo/core-js)
